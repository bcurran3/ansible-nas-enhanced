---
- name: Stop WatchYourLAN
  docker_container:
    name: "{{ watchyourlan_container_name }}"
    state: absent
  when: watchyourlan_enabled is false
  listen:
    - "stop watchyourlan"
    - "stop disabled applications"
