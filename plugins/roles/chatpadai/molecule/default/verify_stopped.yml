---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove Chatpad AI
      community.docker.docker_container:
        name: "{{ chatpadai_container_name }}"
        state: absent
      register: result

    - name: Check Chatpad AI is stopped
      ansible.builtin.assert:
        that:
          - not result.changed
