---
- name: Stop Porn Vault
  docker_container:
    name: "{{ pornvault_container_name }}"
    state: absent
  when: pornvault_enabled is false
  listen:
    - "stop pornvault"
    - "stop disabled applications"
