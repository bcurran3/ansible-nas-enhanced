---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove nvidia_runtime
      community.docker.docker_container:
        name: "{{ nvidia_runtime_container_name }}"
        state: absent
      register: result

    - name: Check nvidia_runtime is stopped
      ansible.builtin.assert:
        that:
          - not result.changed
