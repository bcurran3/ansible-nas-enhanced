---
- name: Stop nvidia_runtime
  docker_container:
    name: "{{ nvidia_runtime_container_name }}"
    state: absent
  when: nvidia_runtime_enabled is false
  listen:
    - "stop nvidia_runtime"
    - "stop disabled applications"
