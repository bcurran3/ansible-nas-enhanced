---
- name: Stop Monitorr
  docker_container:
    name: "{{ monitorr_container_name }}"
    state: absent
  when: monitorr_enabled is false
  listen:
    - "stop monitorr"
    - "stop disabled applications"
