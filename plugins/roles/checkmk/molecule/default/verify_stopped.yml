---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove Checkmk Raw Edition
      community.docker.docker_container:
        name: "{{ checkmk_container_name }}"
        state: absent
      register: result

    - name: Check Checkmk Raw Edition is stopped
      ansible.builtin.assert:
        that:
          - not result.changed
