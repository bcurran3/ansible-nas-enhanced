---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove Christmas Community
      community.docker.docker_container:
        name: "{{ christmas_community_container_name }}"
        state: absent
      register: result

    - name: Check Chirstmas Community is stopped
      ansible.builtin.assert:
        that:
          - not result.changed
