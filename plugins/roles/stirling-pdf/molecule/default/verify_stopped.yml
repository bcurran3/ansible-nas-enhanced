---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove Stirling PDF
      community.docker.docker_container:
        name: "{{ stirling_pdf_container_name }}"
        state: absent
      register: result

    - name: Check Stirling PDF is stopped
      ansible.builtin.assert:
        that:
          - not result.changed
