---
- name: Stop OpenEMR
  docker_container:
    name: "{{ openemr_container_name }}"
    state: absent
  when: openemr_enabled is false
  listen:
    - "stop openemr"
    - "stop disabled applications"
