---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove Resilio Sync
      community.docker.docker_container:
        name: "{{ resilio_sync_container_name }}"
        state: absent
      register: result

    - name: Check Resilio Sync is stopped
      ansible.builtin.assert:
        that:
          - not result.changed
