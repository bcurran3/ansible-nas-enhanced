---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove Paperless-ngx
      community.docker.docker_container:
        name: "{{ paperlessngx_container_name }}"
        state: absent
      register: result

    - name: Check Paperless-ngx is stopped
      ansible.builtin.assert:
        that:
          - not result.changed
