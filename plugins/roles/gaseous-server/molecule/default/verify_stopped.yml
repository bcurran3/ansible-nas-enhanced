---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove Create Gaseous Server
      community.docker.docker_container:
        name: "{{ gaseous_server_container_name }}"
        state: absent
      register: result

    - name: Check Create Gaseous Server is stopped
      ansible.builtin.assert:
        that:
          - not result.changed
