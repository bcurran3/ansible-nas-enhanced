---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove Tdarr Node
      community.docker.docker_container:
        name: "{{ tdarr_node_container_name }}"
        state: absent
      register: result

    - name: Check Tdarr Node is stopped
      ansible.builtin.assert:
        that:
          - not result.changed
