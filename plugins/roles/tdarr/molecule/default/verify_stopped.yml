---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove Tdarr Server
      community.docker.docker_container:
        name: "{{ tdarr_container_name }}"
        state: absent
      register: result

    - name: Check Tdarr Server is stopped
      ansible.builtin.assert:
        that:
          - not result.changed
