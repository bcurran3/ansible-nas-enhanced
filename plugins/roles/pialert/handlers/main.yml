---
- name: Stop Pi.Alert
  docker_container:
    name: "{{ pialert_container_name }}"
    state: absent
  when: pialert_enabled is false
  listen:
    - "stop pialert"
    - "stop disabled applications"
