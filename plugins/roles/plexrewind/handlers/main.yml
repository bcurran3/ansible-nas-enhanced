---
- name: Stop Plex Rewind
  docker_container:
    name: "{{ plexrewind_container_name }}"
    state: absent
  when: plexrewind_enabled is false
  listen:
    - "stop plexrewind"
    - "stop disabled applications"
