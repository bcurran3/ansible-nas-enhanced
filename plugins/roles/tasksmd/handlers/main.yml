---
- name: Stop Tasks.md
  docker_container:
    name: "{{ tasksmd_container_name }}"
    state: absent
  when: tasksmd_enabled is false
  listen:
    - "stop tasksmd"
    - "stop disabled applications"
