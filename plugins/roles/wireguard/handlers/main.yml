---
- name: Stop WireGuard
  docker_container:
    name: "{{ wireguard_container_name }}"
    state: absent
  when: wireguard_enabled is false
  listen:
    - "stop wireguard"
    - "stop disabled applications"
