---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove LANCommander
      community.docker.docker_container:
        name: "{{ lancommander_container_name }}"
        state: absent
      register: result

    - name: Check LANCommander is stopped
      ansible.builtin.assert:
        that:
          - not result.changed
