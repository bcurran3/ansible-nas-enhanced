---
- name: Stop Squirrel Servers Manager (SSM)
  docker_container:
    name: "{{ ssm_container_name }}"
    state: absent
  when: mastadon_enabled is false
  listen:
    - "stop ssm"
    - "stop disabled applications"
