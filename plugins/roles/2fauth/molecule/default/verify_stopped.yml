---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove 2FAuth
      community.docker.docker_container:
        name: "{{ twofauth_container_name }}"
        state: absent
      register: result

    - name: Check 2FAuth is stopped
      ansible.builtin.assert:
        that:
          - not result.changed
