---
- name: Stop SeedSync
  docker_container:
    name: "{{ seedsync_container_name }}"
    state: absent
  when: seedsync_enabled is false
  listen:
    - "stop seedsync"
    - "stop disabled applications"
