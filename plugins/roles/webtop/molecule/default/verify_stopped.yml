---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove Webtop
      community.docker.docker_container:
        name: "{{ webtop_container_name }}"
        state: absent
      register: result

    - name: Check Webtop is stopped
      ansible.builtin.assert:
        that:
          - not result.changed
