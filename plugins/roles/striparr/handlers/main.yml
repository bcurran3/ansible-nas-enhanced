---
- name: Stop Striparr
  docker_container:
    name: "{{ striparr_container_name }}"
    state: absent
  when: striparr_enabled is false
  listen:
    - "stop striparr"
    - "stop disabled applications"
