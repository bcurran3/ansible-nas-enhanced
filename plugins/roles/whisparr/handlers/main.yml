---
- name: Stop Whisparr
  docker_container:
    name: "{{ whisparr_container_name }}"
    state: absent
  when: whisparr_enabled is false
  listen:
    - "stop whisparr"
    - "stop disabled applications"
