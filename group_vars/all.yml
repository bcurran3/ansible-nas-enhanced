---
##      _    _   _ _____
##     / \  | \ | | ____|
##    / _ \ |  \| |  _|
##   / ___ \| |\  | |___
##  /_/   \_\_| \_|_____|
##
##  Ansible-NAS-Enhanced - https://github.com/bcurran3/ansible-nas-enhanced

###
### DO NOT EDIT THIS FILE!
### Add your customisations to inventories/<your_inventory>/group_vars/nas.yml
###

###
### General
###

# User added to the docker group to give user command line access to docker
ansible_nas_user: user

# Your email address, used for Let's Encrypt SSL certs
ansible_nas_email: me@example.com

# Applications will have subdomain SSL certificates created if Traefik is enabled, e.g. ansible-nas.<your-domain>, nextcloud.<your-domain>
ansible_nas_domain: example.com

# Default admin password for apps (where pre-configurable)
ansible_nas_app_admin_password: "topsecret"

# Archive app data when you uninstall the app
archive_app_data_enabled: false

# Where to store Docker volumes
docker_home: /data/docker

# Where to store Docker images
docker_image_directory: "{{ docker_home }}/_images"

# docker-compose support
docker_compose_enabled: false

# Where to keep Docker Compose files (that you provide)
docker_compose_data_directory: "/home/{{ ansible_nas_user }}/docker-compose"

# DNS server for ANE server to use
ansible_nas_dns: 1.1.1.1

# LAN and WAN definitions for traefik IP whitelisting
# you can easily add multiple networks to whitelist, just define them and add them to lan; e.g.:
# vpn_whitelist: 192.168.99.0/16
# lan: "{{ lan_whitelist }}, {{ vpn_whitelist }}"
lan_whitelist: "{{ ansible_default_ipv4.network }}/{{ ansible_default_ipv4.prefix }}"
lan: "{{ lan_whitelist }}"
wan: "0.0.0.0/0"

# AMD GPU support for transcoding and AI
amd_gpu_enabled: false

# Intel iGPU support for transcoding and AI
intel_igpu_enabled: false

# NVIDIA runtime support for transcoding and AI
nvidia_runtime_enabled: false

###
### Media Libraries
###

# The location where all shares will be created by default. Can be overridden on a per-share basis.
samba_shares_root: /data/shares

# Where your anime library is stored
anime_root: "{{ samba_shares_root }}/anime"

# Where your audiobooks library is stored
audiobooks_root: "{{ samba_shares_root }}/audiobooks"

# This path will be mounted to backup containers, Duplicati
backups_root: "{{ samba_shares_root }}/backups"

# Where your books library is stored
books_root: "{{ samba_shares_root }}/books"

# Where your personal repos and code are stored
code_root: "{{ samba_shares_root }}/code"

# Where your comics library is stored
comics_root: "{{ samba_shares_root }}/comics"

# Where your CBT courses library is stored
courses_root: "{{ samba_shares_root }}/courses"

# Where paperless_ng consumes documents
documents_root: "{{ samba_shares_root }}/documents"

# Where your downloaded files are stored
downloads_root: "{{ samba_shares_root }}/downloads"

# Where misc. files are dumped
dump_root: "{{ samba_shares_root }}/dump"

# Where your games library is stored
games_root: "{{ samba_shares_root }}/games"

# Where your movies library is stored
movies_root: "{{ samba_shares_root }}/movies"

# Where your music library is stored
music_root: "{{ samba_shares_root }}/music"

# Where your photos library is stored
photos_root: "{{ samba_shares_root }}/photos"

# Where your podcast library is stored
podcasts_root: "{{ samba_shares_root }}/podcasts"

# Where your porn library is stored
porn_root: "{{ samba_shares_root }}/porn"

# Where you you want public files accessible to all
public_root: "{{ samba_shares_root }}/public"

# Where your video and audio recordings are stored
recordings_root: "{{ samba_shares_root }}/recordings"

# Where your ROMs library is stored
roms_root: "{{ samba_shares_root }}/roms"

# Where your torrent files are stored (picked up by Transmission for downloading)
torrents_root: "{{ samba_shares_root }}/torrents"

# Where your TV episodes library is stored
tv_root: "{{ samba_shares_root }}/tv"

###
### Media Libraries to share over Samba and NFS
###

anime_share_enabled: false
audiobooks_share_enabled: false
backups_share_enabled: false
books_share_enabled: false
code_share_enabled: false
comics_share_enabled: false
courses_share_enabled: false
documents_share_enabled: false
downloads_share_enabled: false
dump_share_enabled: false
games_share_enabled: false
movies_share_enabled: false
music_share_enabled: false
photos_share_enabled: false
podcasts_share_enabled: false
porn_share_enabled: false
public_share_enabled: false
recordings_share_enabled: false
roms_share_enabled: false
torrents_share_enabled: false
tv_share_enabled: false
media_root_share_enabled: false # share everything from one place instead of individually

###
### Samba
###

# Libraries to be published over Samba when set to true (above):
ANE_samba_shares:
  - name: anime
    comment: "Anime"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browsable: yes
    path: "{{ anime_root }}"
    enabled: "{{ anime_share_enabled }}"

  - name: audiobooks
    comment: "Audiobooks"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browsable: yes
    path: "{{ audiobooks_root }}"
    enabled: "{{ audiobooks_share_enabled }}"

  - name: backups
    comment: "Backups"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browseable: yes
    path: "{{ backups_root }}"
    enabled: "{{ backups_share_enabled }}"

  - name: books
    comment: "Books"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browseable: yes
    path: "{{ books_root }}"
    enabled: "{{ books_share_enabled }}"

  - name: code
    comment: "Code"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browseable: yes
    path: "{{ code_root }}"
    enabled: "{{ code_share_enabled }}"

  - name: comics
    comment: "Comics"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browseable: yes
    path: "{{ comics_root }}"
    enabled: "{{ comics_share_enabled }}"

  - name: courses
    comment: "Courses"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browseable: yes
    path: "{{ courses_root }}"
    enabled: "{{ courses_share_enabled }}"

  - name: documents
    comment: "Documents"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browseable: yes
    path: "{{ documents_root }}"
    enabled: "{{ documents_share_enabled }}"

  - name: downloads
    comment: "Downloaded stuff"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browseable: yes
    path: "{{ downloads_root }}"
    enabled: "{{ downloads_share_enabled }}"

  - name: dump
    comment: "File dump"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browseable: yes
    path: "{{ dump_root }}"
    enabled: "{{ dump_share_enabled }}"

  - name: games
    comment: "Games"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browseable: yes
    path: "{{ games_root }}"
    enabled: "{{ games_share_enabled }}"

  - name: movies
    comment: "Movies"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browseable: yes
    path: "{{ movies_root }}"
    enabled: "{{ movies_share_enabled }}"

  - name: music
    comment: "Music"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browseable: yes
    path: "{{ music_root }}"
    enabled: "{{ music_share_enabled }}"

  - name: photos
    comment: "Pictures"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browseable: yes
    path: "{{ photos_root }}"
    enabled: "{{ photos_share_enabled }}"

  - name: podcasts
    comment: "Podcasts"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browseable: yes
    path: "{{ podcasts_root }}"
    enabled: "{{ podcasts_share_enabled }}"

  - name: porn
    comment: "Porn"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browseable: yes
    path: "{{ porn_root }}"
    enabled: "{{ porn_share_enabled }}"

  - name: public
    comment: "Public/Shared files"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browseable: yes
    path: "{{ public_root }}"
    enabled: "{{ public_share_enabled }}"

  - name: recordings
    comment: "Recordings"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browseable: yes
    path: "{{ recordings_root }}"
    enabled: "{{ recordings_share_enabled }}"

  - name: roms
    comment: "ROMs"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browseable: yes
    path: "{{ roms_root }}"
    enabled: "{{ roms_share_enabled }}"

  - name: torrents
    comment: "Torrent Files"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browseable: yes
    path: "{{ torrents_root }}"
    enabled: "{{ torrents_share_enabled }}"

  - name: tv
    comment: "TV Episodes"
    group: ansible-nas
    guest_ok: yes
    public: yes
    writable: yes
    browseable: yes
    path: "{{ tv_root }}"
    enabled: "{{ tv_share_enabled }}"

  - name: media_root
    comment: 'Media'
    group: ansible-nas
    guest_ok: no
    public: yes
    write_list: "{{ ansible_nas_user }}"
    browseable: yes
    path: "{{ samba_shares_root }}"
    enabled: "{{ media_root_share_enabled }}"

# more more details on Samba variables at:
# https://github.com/vladgh/ansible-collection-vladgh-samba

# Enable apple extensions for compatibility with apple clients
samba_apple_extensions: yes

# The account used when Samba shares are accessed. Shouldn't need to change this unless you want to
# mess with Samba user permissions.
samba_guest_account: ansible-nas

# How Samba behaves when an unknown user connects, see Samba docs for more info
samba_map_to_guest: Bad Password

# Seems to break browsing of the \\server-name shares root when enabled
samba_mitigate_cve_2017_7494: false

# The NetBIOS hostname used by Samba on your network
samba_netbios_name: "{{ ansible_nas_hostname }}"

# The description that'll appear next to your Ansible-NAS box when browsing your network
samba_server_string: Ansible NAS Enhanced

###
### NFS
###

# Data you want published over NFS. More info on the NFS config format can be found at
# https://help.ubuntu.com/community/SettingUpNFSHowTo#Shares
# WARNING: Weird things might happen if you share the same data over Samba and NFS and allow writes on both!
# If you're Samba shares are writeable, you should set your NFS shares to read only

#nfs_shares_root: /data/shares - retired
nfs_shares_access: ro # read only, change to rw for read write

# Libraries to be published over NFS when set to true (above):
ANE_nfs_exports_list:
  - export: "{{ anime_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ anime_share_enabled }}"
  - export: "{{ audiobooks_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ audiobooks_share_enabled }}"
  - export: "{{ backups_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ backups_share_enabled }}"
  - export: "{{ books_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ books_share_enabled }}"
  - export: "{{ code_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ code_share_enabled }}"
  - export: "{{ comics_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ comics_share_enabled }}"
  - export: "{{ courses_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ courses_share_enabled }}"
  - export: "{{ documents_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ documents_share_enabled }}"
  - export: "{{ downloads_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ downloads_share_enabled }}"
  - export: "{{ dump_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ dump_share_enabled }}"
  - export: "{{ games_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ games_share_enabled }}"
  - export: "{{ movies_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ movies_share_enabled }}"
  - export: "{{ music_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ music_share_enabled }}"
  - export: "{{ photos_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ photos_share_enabled }}"
  - export: "{{ porn_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ porn_share_enabled }}"
  - export: "{{ public_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ public_share_enabled }}"
  - export: "{{ recordings_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ recordings_share_enabled }}"
  - export: "{{ roms_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ roms_share_enabled }}"
  - export: "{{ torrents_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ torrents_share_enabled }}"
  - export: "{{ tv_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ tv_share_enabled }}"
  - export: "{{ samba_shares_root }} *({{ nfs_shares_access }},sync,no_root_squash)"
    enabled: "{{ media_root_share_enabled }}"

# What version of python ansible should use on target system (path to specific binary)
ansible_python_interpreter: /usr/bin/python3
