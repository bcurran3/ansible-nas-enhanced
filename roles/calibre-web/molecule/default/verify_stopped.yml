---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove {{ calibre_web_proper_name }}
      docker_container:
        name: "{{ calibre_web_container_name }}"
        state: absent
      register: result

    - name: Check {{ calibre_web_proper_name }} is stopped
      assert:
        that:
          - not result.changed
