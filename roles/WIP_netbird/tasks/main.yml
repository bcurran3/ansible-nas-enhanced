---
- name: Start {{ netbird_proper_name }}
  block:
    - name: Create {{ netbird_proper_name }} Directories
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
      with_items:
        - "{{ netbird_data_directory }}"
        - "{{ netbird_data_directory }}/config"
#       - "{{ netbird_data_directory }}/db"

#    - name: Create {{ netbird_proper_name }} Volumes
#      community.docker.docker_volume:
#        name: "{{ item }}"
#      loop:
#        - netbird_volume1

    - name: Set {{ netbird_proper_name }} FQDN var overrides
      ansible.builtin.set_fact:
        netbird_homepage_href: "https://{{ netbird_hostname }}.{{ ansible_nas_domain }}"
      when: netbird_traefik_enabled | bool or netbird_dockflare_enabled | bool

    - name: Create {{ netbird_proper_name }} Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ netbird_container_name }}"
        hostname: "{{ netbird_container_name }}"
        image: "{{ netbird_image_name }}:{{ netbird_image_tag }}"
        pull: true
        volumes:
          - "{{ netbird_data_directory }}/config:/config"
        ports:
          - "{{ netbird_port_http }}:80"
        env:
#         DOCKER_MODS: "{{ netbird_env_docker_mods }}" # linuxserver.io images
#         INSTALL_PACKAGES: "{{ netbird_env_docker_mods_install_packages }}"
#         INSTALL_PIP_PACKAGES: "{{ netbird_env_docker_mods_install_pip_packages}}"
          PUID: "{{ netbird_user_id }}"
          PGID: "{{ netbird_group_id }}"
          TZ: "{{ ansible_nas_timezone }}"
        healthcheck:
          test: "{{ netbird_healthcheck_test }}"
          interval: "{{ netbird_healthcheck_interval }}"
          timeout: "{{ netbird_healthcheck_timeout }}"
          retries: "{{ netbird_healthcheck_retries }}"
          start_period: "{{ netbird_healthcheck_start_period }}"
        labels:
          autoheal: "{{ netbird_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ netbird_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "{{ netbird_dockflare_enabled | ternary('true', 'false') }}"
          dockflare.access.group: "{{ netbird_dockflare_access_group }}"
          dockflare.hostname: "{{ netbird_hostname }}.{{ ansible_nas_domain }}"
          dockflare.service: "http://{{ hostvars[inventory_hostname]['ansible_facts']['default_ipv4']['address'] }}:{{ netbird_port_http }}"
#         dockflare.access.policy: "authenticate"
#         dockflare.access.allowed_idps: "YOUR_IDP_UUID_HERE"
          homepage.name: "{{ netbird_proper_name }}"
          homepage.group: "{{ netbird_homepage_group }}"
          homepage.icon: "{{ netbird_homepage_icon }}"
          homepage.href: "{{ netbird_homepage_href }}"
          homepage.description: "{{ netbird_homepage_description if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ netbird_container_name }}"
          homepage.showStats: "{{ netbird_homepage_showstats | ternary('true', 'false') }}"
          homepage.siteMonitor: "http://{{ hostvars[inventory_hostname]['ansible_facts']['default_ipv4']['address'] }}:{{ netbird_port_http }}"
          repliqate.enabled: "{{ netbird_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ netbird_repliqate_engine }}"
          repliqate.schedule: "{{ netbird_repliqate_schedule }}"
          repliqate.backup_id: "{{ netbird_repliqate_backup_id }}"
          repliqate.retention: "{{ netbird_repliqate_retention }}"
          traefik.enable: "{{ netbird_traefik_enabled | ternary('true', 'false') }}"
          traefik.http.routers.netbird.rule: "Host(`{{ netbird_hostname }}.{{ ansible_nas_domain }}`)"
          traefik.http.routers.netbird.tls.certresolver: "letsencrypt"
          traefik.http.routers.netbird.tls.domains[0].main: "{{ ansible_nas_domain }}"
          traefik.http.routers.netbird.tls.domains[0].sans: "*.{{ ansible_nas_domain }}"
          traefik.http.routers.netbird.middlewares: "netbird@docker{{ ',tinyauth-auth@docker' if netbird_tinyauth_enabled | bool else '' }}"
          traefik.http.middlewares.netbird.IPAllowList.sourcerange: "{{ netbird_traefik_whitelist }}"
          traefik.http.services.netbird.loadbalancer.server.port: "80"
          wud.tag.include: "{{ netbird_wud_tag_include }}"
          wud.tag.transform.type: "{{ netbird_wud_tag_transform_type }}"
          wud.link.template: "{{ netbird_wud_link_template }}"
        cpu_shares: "{{ netbird_cpu_shares }}"
        memory: "{{ netbird_memory }}"
        memory_swap: "{{ netbird_memory_swap }}"
        restart_policy: unless-stopped

    - name: Access {{ netbird_proper_name }} at
      ansible.builtin.debug:
        msg: >-
              {{
                ['http://' + hostvars[inventory_hostname]['ansible_facts']['default_ipv4']['address'] + ':' + (netbird_port_http | string)] +
                (['https://' + netbird_hostname + '.' + ansible_nas_domain] if (netbird_traefik_enabled is true or netbird_dockflare_enabled is true) else [])
              }}
  when: netbird_enabled is true

- name: Stop {{ netbird_proper_name }}
  block:
    - name: Stop {{ netbird_proper_name }} Container
      community.docker.docker_container:
        name: "{{ netbird_container_name }}"
        state: absent
  when: netbird_enabled is false

- name: Data Archive {{ netbird_proper_name }}
  include_role:
    name: ansible-nas-archive
  vars:
    target_data_dir: "{{ netbird_data_directory }}"
  when:
    - archive_app_data_enabled is true
    - netbird_enabled is false

#################################################
###  OPTIONAL - remove if not needed          ###
###           ____ ____  _   _                ###
###          / ___|  _ \| | | |               ###
###         | |  _| |_) | | | |               ###
###         | |_| |  __/| |_| |               ###
###          \____|_|    \___/                ###
#################################################
# if you need GPU support in a container, add this:
# add netbird_amd_ai_enabled: true to the vars if

        device_requests: "{{
          [{'driver': 'nvidia', 'count': -1, 'capabilities': ['gpu']}]
          if nvidia_runtime_enabled | default(false) | bool else omit
        }}"
        devices: >-
          {%- set dev_list = (app_devices | default([])) -%}
          {%- if intel_igpu_enabled | default(false) | bool or amd_gpu_enabled | default(false) | bool -%}
            {%- set _ = dev_list.append('/dev/dri:/dev/dri') -%}
          {%- endif -%}
          {%- if amd_gpu_enabled | default(false) | bool and netbird_amd_ai_enabled | default(false) | bool -%}
            {%- set _ = dev_list.append('/dev/kfd:/dev/kfd') -%}
          {%- endif -%}
          {{ dev_list if dev_list | length > 0 else omit }}
        group_add: "{{ ['render', 'video'] if (amd_gpu_enabled | default(false) | bool or intel_igpu_enabled | default(false) | bool) else omit }}"
        runtime: "{{ 'nvidia' if nvidia_runtime_enabled | default(false) | bool else omit }}"

#################################################
###  OPTIONAL - remove if not needed          ###
###              _                      _     ###
###   _ __   ___| |___      _____  _ __| | __ ###
###  | '_ \ / _ \ __\ \ /\ / / _ \| '__| |/ / ###
###  | | | |  __/ |_ \ V  V / (_) | |  |   <  ###
###  |_| |_|\___|\__| \_/\_/ \___/|_|  |_|\_\ ###
#################################################
## if setting up networks, add this to all containers:
        networks:                                  # optional
          - name: "{{ netbird_network_name }}"     # optional
        network_mode: "{{ netbird_network_name }}" # optional
        networks_cli_compatible: "yes"             # optional

##############################################################################
### EXAMPLES OF COMMON DATABASE SETUPS BELOW
### DELETE ALL OR PARTS BELOW AS NOT NEEDED
### PLEASE STICK TO THIS FORMAT
### NOTE: ../molecule files are not templated for DBs and
###       need to be updated as well when using DBs.
###       Look at the Nextcloud role for a good example.
##############################################################################

#################################################
###  OPTIONAL - remove if not needed          ###
###   __  __            _       ____  ____    ###
###  |  \/  | __ _ _ __(_) __ _|  _ \| __ )   ###
###  | |\/| |/ _` | '__| |/ _` | | | |  _ \   ###
###  | |  | | (_| | |  | | (_| | |_| | |_) |  ###
###  |_|  |_|\__,_|_|  |_|\__,_|____/|____/   ###
#################################################

    - name: Create {{ netbird_proper_name }} DB Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ netbird_db_container_name }}"
        hostname: "{{ netbird_db_container_name }}"
        image: "{{ netbird_db_image_name }}:{{ netbird_db_image_tag }}"
        pull: true
        volumes:
          - "{{ netbird_data_directory }}/db:/var/lib/mysql"
        env:
          PUID: "1000"
          PGID: "1000"
          MYSQL_ROOT_PASSWORD: "topsecretroot"
          MYSQL_DATABASE: "netbird"
          MYSQL_USER: "netbird"
          MYSQL_PASSWORD: "{{ netbird_db_password }}"
          TZ: "{{ ansible_nas_timezone }}"
        healthcheck:
          test: "{{ netbird_db_healthcheck_test }}"
          interval: "{{ netbird_db_healthcheck_interval }}"
          timeout: "{{ netbird_db_healthcheck_timeout }}"
          retries: "{{ netbird_db_healthcheck_retries }}"
          start_period: "{{ netbird_db_healthcheck_start_period }}"
        labels:
          autoheal: "{{ netbird_db_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ netbird_db_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "false"
          homepage.name: "{{ netbird_proper_name }} MariaDB"
          homepage.group: "{{ netbird_db_homepage_group }}"
          homepage.icon: "{{ netbird_db_homepage_icon }}"
          homepage.href: "{{ netbird_db_homepage_href }}"
          homepage.description: "{{ netbird_db_homepage_description if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ netbird_db_container_name }}"
          homepage.showStats: "{{ netbird_db_homepage_showstats | ternary('true', 'false') }}"
          repliqate.enabled: "{{ netbird_db_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ netbird_db_repliqate_engine }}"
          repliqate.schedule: "{{ netbird_db_repliqate_schedule }}"
          repliqate.backup_id: "{{ netbird_db_repliqate_backup_id }}"
          repliqate.retention: "{{ netbird_db_repliqate_retention }}"
          traefik.enable: "false"
          wud.tag.include: "{{ netbird_db_wud_tag_include }}"
          wud.tag.transform.type: "{{ netbird_db_wud_tag_transform_type }}"
          wud.link.template: "{{ netbird_db_wud_link_template }}"
        cpu_shares: "{{ netbird_db_cpu_shares }}"
        memory: "{{ netbird_db_memory }}"
        memory_swap: "{{ netbird_db_memory_swap }}"
        restart_policy: unless-stopped

    - name: Verify {{ netbird_proper_name }} DB is healthy
      community.docker.docker_container_info:
        name: "{{ netbird_db_container_name }}"
      register: db_info
      until: "db_info.container.State.Health.Status == 'healthy'"
      retries: 30
      delay: 5

######################################################
###  OPTIONAL - remove if not needed               ###
###   __  __                         ____  ____    ###
###  |  \/  | ___  _ __   __ _  ___ |  _ \| __ )   ###
###  | |\/| |/ _ \| '_ \ / _` |/ _ \| | | |  _ \   ###
###  | |  | | (_) | | | | (_| | (_) | |_| | |_) |  ###
###  |_|  |_|\___/|_| |_|\__, |\___/|____/|____/   ###
###                      |___/                     ###
######################################################

    - name: Create {{ netbird_proper_name }} DB Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ netbird_db_container_name }}"
        hostname: "{{ netbird_db_container_name }}"
        image: "{{ netbird_db_image_name }}:{{ netbird_db_image_tag }}"
        pull: true
        volumes:
          - "{{ netbird_data_directory }}/db:/data/db"
        env:
          TZ: "{{ ansible_nas_timezone }}"
        healthcheck:
          test: "{{ netbird_db_healthcheck_test }}"
          interval: "{{ netbird_db_healthcheck_interval }}"
          timeout: "{{ netbird_db_healthcheck_timeout }}"
          retries: "{{ netbird_db_healthcheck_retries }}"
          start_period: "{{ netbird_db_healthcheck_start_period }}"
        labels:
          autoheal: "{{ netbird_db_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ netbird_db_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "false"
          homepage.name: "{{ netbird_proper_name }} MongoDB"
          homepage.group: "{{ netbird_db_homepage_group }}"
          homepage.icon: "{{ netbird_db_homepage_icon }}"
          homepage.href: "{{ netbird_db_homepage_href }}"
          homepage.description: "{{ netbird_db_homepage_description if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ netbird_db_container_name }}"
          homepage.showStats: "{{ netbird_db_homepage_showstats | ternary('true', 'false') }}"
          repliqate.enabled: "{{ netbird_db_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ netbird_db_repliqate_engine }}"
          repliqate.schedule: "{{ netbird_db_repliqate_schedule }}"
          repliqate.backup_id: "{{ netbird_db_repliqate_backup_id }}"
          repliqate.retention: "{{ netbird_db_repliqate_retention }}"
          traefik.enable: "false"
          wud.tag.include: "{{ netbird_db_wud_tag_include }}"
          wud.tag.transform.type: "{{ netbird_db_wud_tag_transform_type }}"
          wud.link.template: "{{ netbird_db_wud_link_template }}"
        cpu_shares: "{{ netbird_db_cpu_shares }}"
        memory: "{{ netbird_db_memory }}"
        memory_swap: "{{ netbird_db_memory_swap }}"
        restart_policy: unless-stopped

    - name: Verify {{ netbird_proper_name }} DB is healthy
      community.docker.docker_container_info:
        name: "{{ netbird_db_container_name }}"
      register: db_info
      until: "db_info.container.State.Health.Status == 'healthy'"
      retries: 30
      delay: 5

##########################################
###  OPTIONAL - remove if not needed   ###
###   __  __       ____   ___  _       ###
###  |  \/  |_   _/ ___| / _ \| |      ###
###  | |\/| | | | \___ \| | | | |      ###
###  | |  | | |_| |___) | |_| | |___   ###
###  |_|  |_|\__, |____/ \__\_\_____|  ###
###          |___/                     ###
##########################################

    - name: Create {{ netbird_proper_name }} DB Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ netbird_db_container_name }}"
        hostname: "{{ netbird_db_container_name }}"
        image: "{{ netbird_db_image_name }}:{{ netbird_db_image_tag }}"
        pull: true
        volumes:
          - "{{ netbird_data_directory }}/mysql/mysql:/var/lib/mysql"
          - "{{ netbird_data_directory }}/mysql/my.cnf:/etc/my.cnf"
        env:
          MYSQL_DATABASE: "netbird"
          MYSQL_USER: "{{ netbird_sql_user }}"
          MYSQL_PASSWORD: "{{ netbird_sql_password }}"
          MYSQL_ROOT_PASSWORD: "{{ netbird_sql_root_password }}"
          TZ: "{{ ansible_nas_timezone }}"
        healthcheck:
          test: "{{ netbird_db_healthcheck_test }}"
          interval: "{{ netbird_db_healthcheck_interval }}"
          timeout: "{{ netbird_db_healthcheck_timeout }}"
          retries: "{{ netbird_db_healthcheck_retries }}"
          start_period: "{{ netbird_db_healthcheck_start_period }}"
        labels:
          autoheal: "{{ netbird_db_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ netbird_db_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "false"
          homepage.name: "{{ netbird_proper_name }} MySQL"
          homepage.group: "{{ netbird_db_homepage_group }}"
          homepage.icon: "{{ netbird_db_homepage_icon }}"
          homepage.href: "{{ netbird_db_homepage_href }}"
          homepage.description: "{{ netbird_db_homepage_description if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ netbird_db_container_name }}"
          homepage.showStats: "{{ netbird_db_homepage_showstats | ternary('true', 'false') }}"
          repliqate.enabled: "{{ netbird_db_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ netbird_db_repliqate_engine }}"
          repliqate.schedule: "{{ netbird_db_repliqate_schedule }}"
          repliqate.backup_id: "{{ netbird_db_repliqate_backup_id }}"
          repliqate.retention: "{{ netbird_db_repliqate_retention }}"
          traefik.enable: "false"
          wud.tag.include: "{{ netbird_db_wud_tag_include }}"
          wud.tag.transform.type: "{{ netbird_db_wud_tag_transform_type }}"
          wud.link.template: "{{ netbird_db_wud_link_template }}"
        cpu_shares: "{{ netbird_db_cpu_shares }}"
        memory: "{{ netbird_db_memory }}"
        memory_swap: "{{ netbird_db_memory_swap }}"
        restart_policy: unless-stopped

    - name: Verify {{ netbird_proper_name }} DB is healthy
      community.docker.docker_container_info:
        name: "{{ netbird_db_container_name }}"
      register: db_info
      until: "db_info.container.State.Health.Status == 'healthy'"
      retries: 30
      delay: 5

##############################################################
###  OPTIONAL - remove if not needed                       ###
###   ____           _                 ____   ___  _       ###
###  |  _ \ ___  ___| |_ __ _ _ __ ___/ ___| / _ \| |      ###
###  | |_) / _ \/ __| __/ _` | '__/ _ \___ \| | | | |      ###
###  |  __/ (_) \__ \ || (_| | | |  __/___) | |_| | |___   ###
###  |_|   \___/|___/\__\__, |_|  \___|____/ \__\_\_____|  ###
###                     |___/                              ###
##############################################################

    - name: Create {{ netbird_proper_name }} DB Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ netbird_db_container_name }}"
        hostname: "{{ netbird_db_container_name }}"
        image: "{{ netbird_db_image_name }}:{{ netbird_db_image_tag }}"
        pull: true
        volumes:
          - "{{ netbird_data_directory }}/db:/var/lib/postgresql/data"
        env:
          POSTGRES_USER: "netbird"
          POSTGRES_PASSWORD: "netbird"
          POSTGRES_DB: "netbird"
          TZ: "{{ ansible_nas_timezone }}"
        healthcheck:
          test: "{{ netbird_db_healthcheck_test }}"
          interval: "{{ netbird_db_healthcheck_interval }}"
          timeout: "{{ netbird_db_healthcheck_timeout }}"
          retries: "{{ netbird_db_healthcheck_retries }}"
          start_period: "{{ netbird_db_healthcheck_start_period }}"
        labels:
          autoheal: "{{ netbird_db_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ netbird_db_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "false"
          homepage.name: "{{ netbird_proper_name }} PostgreSQL"
          homepage.group: "{{ netbird_db_homepage_group }}"
          homepage.icon: "{{ netbird_db_homepage_icon }}"
          homepage.href: "{{ netbird_db_homepage_href }}"
          homepage.description: "{{ netbird_db_homepage_description if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ netbird_db_container_name }}"
          homepage.showStats: "{{ netbird_db_homepage_showstats | ternary('true', 'false') }}"
          repliqate.enabled: "{{ netbird_db_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ netbird_db_repliqate_engine }}"
          repliqate.schedule: "{{ netbird_db_repliqate_schedule }}"
          repliqate.backup_id: "{{ netbird_db_repliqate_backup_id }}"
          repliqate.retention: "{{ netbird_db_repliqate_retention }}"
          traefik.enable: "false"
          wud.tag.include: "{{ netbird_db_wud_tag_include }}"
          wud.tag.transform.type: "{{ netbird_db_wud_tag_transform_type }}"
          wud.link.template: "{{ netbird_db_wud_link_template }}"
        cpu_shares: "{{ netbird_db_cpu_shares }}"
        memory: "{{ netbird_db_memory }}"
        memory_swap: "{{ netbird_db_memory_swap }}"
        restart_policy: unless-stopped

    - name: Verify {{ netbird_proper_name }} DB is healthy
      community.docker.docker_container_info:
        name: "{{ netbird_db_container_name }}"
      register: db_info
      until: "db_info.container.State.Health.Status == 'healthy'"
      retries: 30
      delay: 5

#########################################
###  OPTIONAL - remove if not needed  ###
###       ____          _ _           ###
###      |  _ \ ___  __| (_)___       ###
###      | |_) / _ \/ _` | / __|      ###
###      |  _ <  __/ (_| | \__ \      ###
###      |_| \_\___|\__,_|_|___/      ###
#########################################

    - name: Create {{ netbird_proper_name }} Redis Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ netbird_redis_container_name }}"
        hostname: "{{ netbird_redis_container_name }}"
        image: "{{ netbird_redis_image_name }}:{{ netbird_redis_image_tag }}"
        pull: true
        command: "redis-server --requirepass topsecret"
        env:
          TZ: "{{ ansible_nas_timezone }}"
        healthcheck:
          test: "{{ netbird_redis_healthcheck_test }}"
          interval: "{{ netbird_redis_healthcheck_interval }}"
          timeout: "{{ netbird_redis_healthcheck_timeout }}"
          retries: "{{ netbird_redis_healthcheck_retries }}"
          start_period: "{{ netbird_redis_healthcheck_start_period }}"
        labels:
          autoheal: "{{ netbird_redis_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ netbird_redis_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "false"
          homepage.name: "{{ netbird_proper_name }} Redis"
          homepage.group: "{{ netbird_redis_homepage_group }}"
          homepage.icon: "{{ netbird_redis_homepage_icon }}"
          homepage.href: "{{ netbird_redis_homepage_href }}"
          homepage.description: "{{ netbird_redis_homepage_description if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ netbird_redis_container_name }}"
          homepage.showStats: "{{ netbird_redis_homepage_showstats | ternary('true', 'false') }}"
          repliqate.enabled: "{{ netbird_redis_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ netbird_redis_repliqate_engine }}"
          repliqate.schedule: "{{ netbird_redis_repliqate_schedule }}"
          repliqate.backup_id: "{{ netbird_redis_repliqate_backup_id }}"
          repliqate.retention: "{{ netbird_redis_repliqate_retention }}"
          traefik.enable: "false"
          wud.tag.include: "{{ netbird_redis_wud_tag_include }}"
          wud.tag.transform.type: "{{ netbird_redis_wud_tag_transform_type }}"
          wud.link.template: "{{ netbird_redis_wud_link_template }}"
        cpu_shares: "{{ netbird_redis_cpu_shares }}"
        memory: "{{ netbird_redis_memory }}"
        memory_swap: "{{ netbird_redis_memory_swap }}"
        restart_policy: unless-stopped
