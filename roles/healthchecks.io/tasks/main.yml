---
- name: Start {{ healthchecks.io_proper_name }}
  block:
    - name: Add {{ healthchecks.io_proper_name }} cronjob
      ansible.builtin.cron:
        name: healthchecks.io
        minute: "*/{{ healthchecks_ping_minutes }}"
        user: root
        job: "curl -m 10 --retry 5 {{ healthchecks_url }}"
  when: healthchecks_enabled is true

#- name: Stop {{ healthchecks.io_proper_name }}
#  block:
#    - name: Remove {{ healthchecks.io_proper_name }} cronjob
#      ansible.builtin.cron:
#        name: healthchecks.io
#        user: root
#        state: absent
#  when: healthchecks_enabled is false

# ^ original code causes failure
