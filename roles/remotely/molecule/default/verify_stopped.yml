---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove Remot{{ remotely_proper_name }}ely
      community.docker.docker_container:
        name: "{{ remotely_container_name }}"
        state: absent
      register: result

    - name: Check {{ remotely_proper_name }} is stopped
      ansible.builtin.assert:
        that:
          - not result.changed
