---
- name: Start {{ ansible_nas_app_name }}
  block:
    - name: Create {{ ansible_nas_app_name }} Directories
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
      with_items:
        - "{{ mediacms_data_directory }}"

    - name: Create {{ ansible_nas_app_name }} Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ mediacms_container_name }}"
        hostname: "{{ mediacms_container_name }}"
        image: "{{ mediacms_image_name }}:{{ mediacms_image_tag }}"
        pull: true
        volumes:
          - "{{ mediacms_data_directory }}:/config"
        ports:
          - "{{ mediacms_port_http }}:80"
        env:
#         DOCKER_MODS: "{{ mediacms_env_docker_mods }}" # linuxserver.io images
#         INSTALL_PACKAGES: "{{ mediacms_env_docker_mods_install_packages }}"
#         INSTALL_PIP_PACKAGES: "{{ mediacms_env_docker_mods_install_pip_packages}}"
          PUID: "{{ mediacms_user_id }}"
          PGID: "{{ mediacms_group_id }}"
          TZ: "{{ ansible_nas_timezone }}"
        labels:
          autoheal: "{{ mediacms_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ mediacms_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "{{ mediacms_dockflare_enabled | ternary('true', 'false') }}"
          dockflare.access.group: "{{ mediacms_dockflare_access_group }}"
          dockflare.hostname: "{{ mediacms_hostname }}.{{ ansible_nas_domain }}"
          dockflare.service: "http://{{ hostvars[inventory_hostname]['ansible_default_ipv4']['address'] }}:{{ mediacms_port_http }}"
#         dockflare.access.policy: "authenticate"
#         dockflare.access.allowed_idps: "YOUR_IDP_UUID_HERE"
          homepage.name: "{{ ansible_nas_app_name }}"
          homepage.group: "{{ mediacms_homepage_group }}"
          homepage.icon: "{{ mediacms_homepage_icon }}"
          homepage.href: "{{ mediacms_homepage_href }}"
          homepage.description: "{{ 'appname description' if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ mediacms_container_name }}"
          homepage.showStats: "{{ mediacms_homepage_showstats | ternary('true', 'false') }}"
          homepage.siteMonitor: "http://{{ hostvars[inventory_hostname]['ansible_default_ipv4']['address'] }}:{{ mediacms_port_http }}"
          repliqate.enabled: "{{ mediacms_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ mediacms_repliqate_engine }}"
          repliqate.schedule: "{{ mediacms_repliqate_schedule }}"
          repliqate.backup_id: "{{ mediacms_repliqate_backup_id }}"
          repliqate.retention: "{{ mediacms_repliqate_retention }}"
          traefik.enable: "{{ mediacms_traefik_enabled | ternary('true', 'false') }}"
          traefik.http.routers.appname.rule: "Host(`{{ mediacms_hostname }}.{{ ansible_nas_domain }}`)"
          traefik.http.routers.appname.tls.certresolver: "letsencrypt"
          traefik.http.routers.appname.tls.domains[0].main: "{{ ansible_nas_domain }}"
          traefik.http.routers.appname.tls.domains[0].sans: "*.{{ ansible_nas_domain }}"
          traefik.http.routers.appname.middlewares: "appname@docker{{ ',tinyauth-auth@docker' if mediacms_tinyauth_enabled | bool else '' }}"
          traefik.http.middlewares.appname.IPAllowList.sourcerange: "{{ mediacms_traefik_whitelist }}"
          traefik.http.services.appname.loadbalancer.server.port: "80"
### Docker hub example
          wud.tag.include: '^\d+\.\d+$'
          wud.tag.transform.type: "semver"
          wud.link.template: "https://hub.docker.com/r/devname/appname"
### Github example
#          wud.tag.include: '^\d+\.\d+\.\d+$'
#          wud.tag.transform.type: "semver"
#          wud.link.template: "https://github.com/dev/appname/pkgs/container/appname"
### Linuxserver.io example
#          wud.tag.include: '^\d+\.\d+\.\d+.*$'
#          wud.tag.transform.type: "semver"
#          wud.link.template: "https://lscr.io/linuxserver/appname"
        cpu_shares: "{{ mediacms_cpu_shares }}"
        memory: "{{ mediacms_memory }}"
        restart_policy: unless-stopped

    - name: Access {{ ansible_nas_app_name }} at
      ansible.builtin.debug:
        msg: >-
              {{
                ['http://' + hostvars[inventory_hostname]['ansible_default_ipv4']['address'] + ':' + (mediacms_port_http | string)] +
                (['https://' + mediacms_hostname + '.' + ansible_nas_domain] if (mediacms_traefik_enabled is true or mediacms_dockflare_enabled is true) else [])
              }}
  when: mediacms_enabled is true

- name: Stop {{ ansible_nas_app_name }}
  block:
    - name: Stop {{ ansible_nas_app_name }} Container
      community.docker.docker_container:
        name: "{{ mediacms_container_name }}"
        state: absent
  when: mediacms_enabled is false

- name: Data Archive {{ ansible_nas_app_name }}
  include_role:
    name: ansible-nas-archive
  vars:
    target_data_dir: "{{ mediacms_data_directory }}"
  when:
    - archive_app_data_enabled is true
    - mediacms_enabled is false



##############################################################################
### EXAMPLES OF COMMON DATABASE SETUPS BELOW
### DELETE ALL OR PARTS BELOW AS NOT NEEDED
### PLEASE STICK TO THIS FORMAT
### NOTE: ../molecule files are not templated for DBs and
###       need to be updated as well when using DBs.
###       Look at the Nextcloud role for a good example.
##############################################################################


#################################################
###  OPTIONAL - remove if not needed          ###
###   __  __            _       ____  ____    ###
###  |  \/  | __ _ _ __(_) __ _|  _ \| __ )   ###
###  | |\/| |/ _` | '__| |/ _` | | | |  _ \   ###
###  | |  | | (_| | |  | | (_| | |_| | |_) |  ###
###  |_|  |_|\__,_|_|  |_|\__,_|____/|____/   ###
#################################################

    - name: Create {{ ansible_nas_app_name }} DB Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ mediacms_db_container_name }}"
        hostname: "{{ mediacms_db_container_name }}"
        image: "{{ mediacms_db_image_name }}:{{ mediacms_db_image_tag }}"
        pull: true
        networks:                                  # optional
          - name: "{{ mediacms_network_name }}"     # optional
        network_mode: "{{ mediacms_network_name }}" # optional
        networks_cli_compatible: "yes"             # optional
        volumes:
          - "{{ mediacms_data_directory }}/db:/var/lib/mysql"
        env:
          PUID: "1000"
          PGID: "1000"
          MYSQL_ROOT_PASSWORD: "topsecretroot"
          MYSQL_DATABASE: "appname"
          MYSQL_USER: "appname"
          MYSQL_PASSWORD: "{{ mediacms_db_password }}"
          TZ: "{{ ansible_nas_timezone }}"
        healthcheck:
              test:
                - CMD
                - /usr/local/bin/healthcheck.sh
                - --su-mysql
                - --connect
                - --innodb_initialized
              interval: "30s"
              timeout: "10s"
              retries: "3"
              start_period: "30s"
        labels:
          autoheal: "{{ mediacms_db_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ mediacms_db_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "false"
          homepage.name: "{{ ansible_nas_app_name }} MariaDB"
          homepage.group: "{{ mediacms_db_homepage_group }}"
          homepage.icon: "{{ mediacms_db_homepage_icon }}"
          homepage.href: "{{ mediacms_homepage_href }}"
          homepage.description: "{{ 'MariaDB Server is a high performing open source relational database, forked from MySQL.' if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ mediacms_db_container_name }}"
          homepage.showStats: "{{ mediacms_db_homepage_showstats | ternary('true', 'false') }}"
          repliqate.enabled: "{{ mediacms_db_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ mediacms_db_repliqate_engine }}"
          repliqate.schedule: "{{ mediacms_db_repliqate_schedule }}"
          repliqate.backup_id: "{{ mediacms_db_repliqate_backup_id }}"
          repliqate.retention: "{{ mediacms_db_repliqate_retention }}"
          traefik.enable: "false"
          wud.tag.include: '^\d+\.\d+\.\d+.*$'
          wud.tag.transform.type: "semver"
          wud.link.template: "https://hub.docker.com/_/mariadb"
        cpu_shares: "{{ mediacms_db_cpu_shares }}"
        memory: "{{ mediacms_db_memory }}"
        restart_policy: unless-stopped


######################################################
###  OPTIONAL - remove if not needed               ###
###   __  __                         ____  ____    ###
###  |  \/  | ___  _ __   __ _  ___ |  _ \| __ )   ###
###  | |\/| |/ _ \| '_ \ / _` |/ _ \| | | |  _ \   ###
###  | |  | | (_) | | | | (_| | (_) | |_| | |_) |  ###
###  |_|  |_|\___/|_| |_|\__, |\___/|____/|____/   ###
###                      |___/                     ###
######################################################

    - name: Create {{ ansible_nas_app_name }} DB Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ mediacms_db_container_name }}"
        hostname: "{{ mediacms_db_container_name }}"
        image: "{{ mediacms_db_image_name }}:{{ mediacms_db_image_tag }}"
        pull: true
        networks:                                  # optional
          - name: "{{ mediacms_network_name }}"     # optional
        network_mode: "{{ mediacms_network_name }}" # optional
        networks_cli_compatible: "yes"             # optional
        volumes:
          - "{{ mediacms_data_directory }}/db:/data/db"
        env:
          TZ: "{{ ansible_nas_timezone }}"
        labels:
          autoheal: "{{ mediacms_db_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ mediacms_db_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "false"
          homepage.name: "{{ ansible_nas_app_name }} MongoDB"
          homepage.group: "{{ mediacms_db_homepage_group }}"
          homepage.icon: "{{ mediacms_db_homepage_icon }}"
          homepage.href: "{{ mediacms_homepage_href }}"
          homepage.description: "{{ 'The Database for Secure Apps' if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ mediacms_db_container_name }}"
          homepage.showStats: "{{ mediacms_db_homepage_showstats | ternary('true', 'false') }}"
          repliqate.enabled: "{{ mediacms_db_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ mediacms_db_repliqate_engine }}"
          repliqate.schedule: "{{ mediacms_db_repliqate_schedule }}"
          repliqate.backup_id: "{{ mediacms_db_repliqate_backup_id }}"
          repliqate.retention: "{{ mediacms_db_repliqate_retention }}"
          traefik.enable: "false"
          wud.tag.include: '^\d+\.\d+\.\d+.*$'
          wud.tag.transform.type: "semver"
          wud.link.template: "https://hub.docker.com/_/mongo"
        cpu_shares: "{{ mediacms_db_cpu_shares }}"
        memory: "{{ mediacms_db_memory }}"
        restart_policy: unless-stopped


##########################################
###  OPTIONAL - remove if not needed   ###
###   __  __       ____   ___  _       ###
###  |  \/  |_   _/ ___| / _ \| |      ###
###  | |\/| | | | \___ \| | | | |      ###
###  | |  | | |_| |___) | |_| | |___   ###
###  |_|  |_|\__, |____/ \__\_\_____|  ###
###          |___/                     ###
##########################################

    - name: Create {{ ansible_nas_app_name }} DB Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ mediacms_db_container_name }}"
        hostname: "{{ mediacms_db_container_name }}"
        image: "{{ mediacms_db_image_name }}:{{ mediacms_db_image_tag }}"
        pull: true
        networks:                                  # optional
          - name: "{{ mediacms_network_name }}"     # optional
        network_mode: "{{ mediacms_network_name }}" # optional
        networks_cli_compatible: "yes"             # optional
        volumes:
          - "{{ mediacms_data_directory }}/mysql/mysql:/var/lib/mysql"
          - "{{ mediacms_data_directory }}/mysql/my.cnf:/etc/my.cnf"
        env:
          MYSQL_DATABASE: "appname"
          MYSQL_USER: "{{ mediacms_sql_user }}"
          MYSQL_PASSWORD: "{{ mediacms_sql_password }}"
          MYSQL_ROOT_PASSWORD: "{{ mediacms_sql_root_password }}"
          TZ: "{{ ansible_nas_timezone }}"
        labels:
          autoheal: "{{ mediacms_db_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ mediacms_db_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "false"
          homepage.name: "{{ ansible_nas_app_name }} MySQL"
          homepage.group: "{{ mediacms_db_homepage_group }}"
          homepage.icon: "{{ mediacms_db_homepage_icon }}"
          homepage.href: "{{ mediacms_homepage_href }}"
          homepage.description: "{{ 'MySQL Server, the world''s most popular open source database' if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ mediacms_db_container_name }}"
          homepage.showStats: "{{ mediacms_db_homepage_showstats | ternary('true', 'false') }}"
          repliqate.enabled: "{{ mediacms_db_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ mediacms_db_repliqate_engine }}"
          repliqate.schedule: "{{ mediacms_db_repliqate_schedule }}"
          repliqate.backup_id: "{{ mediacms_db_repliqate_backup_id }}"
          repliqate.retention: "{{ mediacms_db_repliqate_retention }}"
          traefik.enable: "false"
          wud.tag.include: '^\d+\.\d+\.\d+.*$'
          wud.tag.transform.type: "semver"
          wud.link.template: "https://hub.docker.com/_/mysql"
        cpu_shares: "{{ mediacms_db_cpu_shares }}"
        memory: "{{ mediacms_db_memory }}"
        restart_policy: unless-stopped


##############################################################
###  OPTIONAL - remove if not needed                       ###
###   ____           _                 ____   ___  _       ###
###  |  _ \ ___  ___| |_ __ _ _ __ ___/ ___| / _ \| |      ###
###  | |_) / _ \/ __| __/ _` | '__/ _ \___ \| | | | |      ###
###  |  __/ (_) \__ \ || (_| | | |  __/___) | |_| | |___   ###
###  |_|   \___/|___/\__\__, |_|  \___|____/ \__\_\_____|  ###
###                     |___/                              ###
##############################################################

    - name: Create {{ ansible_nas_app_name }} DB Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ mediacms_db_container_name }}"
        hostname: "{{ mediacms_db_container_name }}"
        image: "{{ mediacms_db_image_name }}:{{ mediacms_db_image_tag }}"
        pull: true
        networks:                                  # optional
          - name: "{{ mediacms_network_name }}"     # optional
        network_mode: "{{ mediacms_network_name }}" # optional
        networks_cli_compatible: "yes"             # optional
        volumes:
          - "{{ mediacms_data_directory }}/db:/var/lib/postgresql/data"
        env:
          POSTGRES_USER: "appname"
          POSTGRES_PASSWORD: "appname"
          POSTGRES_DB: "appname"
          TZ: "{{ ansible_nas_timezone }}"
        labels:
          autoheal: "{{ mediacms_db_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ mediacms_db_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "false"
          homepage.name: "{{ ansible_nas_app_name }} PostgreSQL"
          homepage.group: "{{ mediacms_db_homepage_group }}"
          homepage.icon: "{{ mediacms_db_homepage_icon }}"
          homepage.href: "{{ mediacms_homepage_href }}"
          homepage.description: "{{ 'PostgreSQL Database Management System' if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ mediacms_db_container_name }}"
          homepage.showStats: "{{ mediacms_db_homepage_showstats | ternary('true', 'false') }}"
          repliqate.enabled: "{{ mediacms_db_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ mediacms_db_repliqate_engine }}"
          repliqate.schedule: "{{ mediacms_db_repliqate_schedule }}"
          repliqate.backup_id: "{{ mediacms_db_repliqate_backup_id }}"
          repliqate.retention: "{{ mediacms_db_repliqate_retention }}"
          traefik.enable: "false"
          wud.tag.include: '^\d+\.\d+.*$'
          wud.tag.transform.type: "semver"
          wud.link.template: "https://hub.docker.com/_/postgres"
        cpu_shares: "{{ mediacms_db_cpu_shares }}"
        memory: "{{ mediacms_db_memory }}"
        restart_policy: unless-stopped


#########################################
###  OPTIONAL - remove if not needed  ###
###       ____          _ _           ###
###      |  _ \ ___  __| (_)___       ###
###      | |_) / _ \/ _` | / __|      ###
###      |  _ <  __/ (_| | \__ \      ###
###      |_| \_\___|\__,_|_|___/      ###
#########################################

    - name: Create {{ ansible_nas_app_name }} Redis Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ mediacms_redis_container_name }}"
        hostname: "{{ mediacms_redis_container_name }}"
        image: "{{ mediacms_redis_image_name }}:{{ mediacms_redis_image_tag }}"
        pull: true
        networks:                                  # optional
          - name: "{{ mediacms_network_name }}"     # optional
        network_mode: "{{ mediacms_network_name }}" # optional
        networks_cli_compatible: "yes"             # optional
        command: "redis-server --requirepass topsecret"
        env:
          TZ: "{{ ansible_nas_timezone }}"
        labels:
          autoheal: "{{ mediacms_redis_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ mediacms_redis_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "false"
          homepage.name: "{{ ansible_nas_app_name }} Redis"
          homepage.group: "{{ mediacms_redis_homepage_group }}"
          homepage.icon: "{{ mediacms_redis_homepage_icon }}"
          homepage.href: "{{ mediacms_homepage_href }}"
          homepage.description: "{{ 'The Real-time Data Platform' if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ mediacms_redis_container_name }}"
          homepage.showStats: "{{ mediacms_redis_homepage_showstats | ternary('true', 'false') }}"
          repliqate.enabled: "{{ mediacms_redis_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ mediacms_redis_repliqate_engine }}"
          repliqate.schedule: "{{ mediacms_redis_repliqate_schedule }}"
          repliqate.backup_id: "{{ mediacms_redis_repliqate_backup_id }}"
          repliqate.retention: "{{ mediacms_redis_repliqate_retention }}"
          traefik.enable: "false"
          wud.tag.include: '^\d+\.\d+\.\d+.*$'
          wud.tag.transform.type: "semver"
          wud.link.template: "https://hub.docker.com/_/redis"
        cpu_shares: "{{ mediacms_redis_cpu_shares }}"
        memory: "{{ mediacms_redis_memory }}"
        restart_policy: unless-stopped
