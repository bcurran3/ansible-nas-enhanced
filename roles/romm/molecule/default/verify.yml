---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Get {{ romm_proper_name }} container state
      community.docker.docker_container:
        name: "{{ romm_container_name }}"
      register: result

    - name: Get {{ romm_proper_name }} DB container state
      community.docker.docker_container:
        name: "{{ romm_db_container_name }}"
      register: result_db

    - name: Get {{ romm_proper_name }} Redis container state
      community.docker.docker_container:
        name: "{{ romm_redis_container_name }}"
      register: result_redis

    - name: Check if {{ romm_proper_name }} containers are running
      ansible.builtin.assert:
        that:
          - result.container['State']['Status'] == "running"
          - result.container['State']['Restarting'] == false
          - result_db.container['State']['Status'] == "running"
          - result_db.container['State']['Restarting'] == false
          - result_redis.container['State']['Status'] == "running"
          - result_redis.container['State']['Restarting'] == false
