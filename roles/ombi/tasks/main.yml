---
- name: Start Ombi
  block:
    - name: Create {{ ansible_nas_app_name }} Directories
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
      with_items:
        - "{{ ombi_data_directory }}"
        - "{{ ombi_data_directory }}/config"

    - name: Create {{ ansible_nas_app_name }} Container
      community.docker.docker_container:
        name: "{{ ombi_container_name }}"
        image: "{{ ombi_image_name }}:{{ ombi_image_version }}"
        pull: true
        cpu_shares: "{{ ombi_cpu_shares }}"
        volumes:
          - "{{ ombi_data_directory }}/config:/config:rw"
        ports:
          - "{{ ombi_port }}:3579"
        env:
          TZ: "{{ ansible_nas_timezone }}"
          PUID: "{{ ombi_user_id }}"
          PGID: "{{ ombi_group_id }}"
        restart_policy: unless-stopped
        memory: "{{ ombi_memory }}"
        labels:
          autoheal: "{{ ombi_autoheal }}"
          com.centurylinklabs.watchtower.monitor-only: "{{ ombi_watchtower_monitor_only }}"
          traefik.enable: "{{ ombi_available_externally | string }}"
          traefik.http.routers.ombi.rule: "Host(`{{ ombi_hostname }}.{{ ansible_nas_domain }}`)"
          traefik.http.routers.ombi.tls.certresolver: "letsencrypt"
          traefik.http.routers.ombi.tls.domains[0].main: "{{ ansible_nas_domain }}"
          traefik.http.routers.ombi.tls.domains[0].sans: "*.{{ ansible_nas_domain }}"
          traefik.http.services.ombi.loadbalancer.server.port: "3579"
  when: ombi_enabled is true

- name: Stop {{ ansible_nas_app_name }}
  block:
    - name: Stop {{ ansible_nas_app_name }} Container
      community.docker.docker_container:
        name: "{{ ombi_container_name }}"
        state: absent
  when: ombi_enabled is false

- name: Data Archive {{ ansible_nas_app_name }}
  block:
    - name: Check for {{ ansible_nas_app_name }} Data Directory
      stat:
        path: "{{ ombi_data_directory }}"
      register: dir_stat

    - name: Archive {{ ansible_nas_app_name }} Data Directory
      ansible.builtin.archive:
        path: "{{ ombi_data_directory }}"
        dest: "{{ docker_home }}/{{ ansible_nas_app_name | lower }}.zip"
        force_archive: true
        format: zip
        owner: ansible-nas
        group: ansible-nas
        mode: u=rw,g=rw
        remove: true
      when: dir_stat.stat.exists
  when: ombi_enabled is false and archive_app_data_enabled is true
