---
- name: Install and start {{ ansible_nas_app_name }}
  block:
    - name: Add Hashicorp GPG key
      ansible.builtin.get_url:
        url: https://apt.releases.hashicorp.com/gpg
        dest: /etc/apt/trusted.gpg.d/hashicorp.asc
        mode: '0644'
        force: true

    - name: Add Hashicorp apt repository
      ansible.builtin.apt_repository:
        repo: "deb https://apt.releases.hashicorp.com {{ ansible_facts['lsb']['codename'] }} main"
        state: present
        filename: hashicorp

    - name: Install {{ ansible_nas_app_name }}
      ansible.builtin.apt:
        name: nomad
        state: present

    - name: Copy {{ ansible_nas_app_name }} config
      ansible.builtin.template:
        src: nomad.hcl
        dest: /etc/nomad.d/nomad.hcl
      notify: restart nomad

    - name: Start {{ ansible_nas_app_name }} Service
      ansible.builtin.systemd:
        name: "{{ nomad_service_name }}"
        state: started
        enabled: yes
  when: nomad_enabled is true

- name: Stop {{ ansible_nas_app_name }}
  block:
    - name: Check if {{ ansible_nas_app_name }} is installed
      ansible.builtin.stat:
        path: /usr/bin/nomad
      register: nomad_install

    - name: Stop and Uninstall {{ ansible_nas_app_name }}
      block:
        - name: Stop {{ ansible_nas_app_name }} Service
          ansible.builtin.systemd:
            name: "{{ nomad_service_name }}"
            state: stopped
            enabled: no
            daemon_reload: yes

        - name: Uninstall {{ ansible_nas_app_name }}
          ansible.builtin.apt:
            name: nomad
            state: absent
      when: nomad_install.stat.exists
  when: nomad_enabled is false
