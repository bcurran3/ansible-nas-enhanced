---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Get container state {{ ansible_nas_app_name }} Web UI
      community.docker.docker_container_info:
        name: "{{ magneticow_container_name }}"
      register: result

    - name: Get container state {{ ansible_nas_app_name }} Crawler
      community.docker.docker_container_info:
        name: "{{ magneticod_container_name }}"
      register: resultb

    - name: Check {{ ansible_nas_app_name }} is running
      ansible.builtin.assert:
        that:
          - result.container['State']['Status'] == "running"
          - result.container['State']['Restarting'] == false
          - resultb.container['State']['Status'] == "running"
          - resultb.container['State']['Restarting'] == false
