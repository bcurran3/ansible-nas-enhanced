---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove {{ many_notes_proper_name }}
      community.docker.docker_container:
        name: "{{ many_notes_container_name }}"
        state: absent
      register: result

    - name: Check if {{ many_notes_proper_name }} is stopped
      ansible.builtin.assert:
        that:
          - not result.changed
