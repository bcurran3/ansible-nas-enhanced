---
- name: Start {{ ansible_nas_app_name }}
  block:
    - name: Create {{ ansible_nas_app_name }} Directories
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
      with_items:
        - "{{ mattermost_data_directory }}"

    - name: Create {{ ansible_nas_app_name }} Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ mattermost_container_name }}"
        hostname: "{{ mattermost_container_name }}"
        image: "{{ mattermost_image_name }}:{{ mattermost_image_tag }}"
        pull: true
        volumes:
          - "{{ mattermost_data_directory }}:/config"
        ports:
          - "{{ mattermost_port_http }}:80"
        env:
#         DOCKER_MODS: "{{ mattermost_env_docker_mods }}" # linuxserver.io images
#         INSTALL_PACKAGES: "{{ mattermost_env_docker_mods_install_packages }}"
#         INSTALL_PIP_PACKAGES: "{{ mattermost_env_docker_mods_install_pip_packages}}"
          PUID: "{{ mattermost_user_id }}"
          PGID: "{{ mattermost_group_id }}"
          TZ: "{{ ansible_nas_timezone }}"
        labels:
          autoheal: "{{ mattermost_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ mattermost_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "{{ mattermost_dockflare_enabled | ternary('true', 'false') }}"
          dockflare.access.group: "{{ mattermost_dockflare_access_group }}"
          dockflare.hostname: "{{ mattermost_hostname }}.{{ ansible_nas_domain }}"
          dockflare.service: "http://{{ hostvars[inventory_hostname]['ansible_default_ipv4']['address'] }}:{{ mattermost_port_http }}"
#         dockflare.access.policy: "authenticate"
#         dockflare.access.allowed_idps: "YOUR_IDP_UUID_HERE"
          homepage.name: "{{ ansible_nas_app_name }}"
          homepage.group: "{{ mattermost_homepage_group }}"
          homepage.icon: "{{ mattermost_homepage_icon }}"
          homepage.href: "{{ mattermost_homepage_href }}"
          homepage.description: "{{ 'appname description' if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ mattermost_container_name }}"
          homepage.showStats: "{{ mattermost_homepage_showstats | ternary('true', 'false') }}"
          homepage.siteMonitor: "http://{{ hostvars[inventory_hostname]['ansible_default_ipv4']['address'] }}:{{ mattermost_port_http }}"
          repliqate.enabled: "{{ mattermost_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ mattermost_repliqate_engine }}"
          repliqate.schedule: "{{ mattermost_repliqate_schedule }}"
          repliqate.backup_id: "{{ mattermost_repliqate_backup_id }}"
          repliqate.retention: "{{ mattermost_repliqate_retention }}"
          traefik.enable: "{{ mattermost_traefik_enabled | ternary('true', 'false') }}"
          traefik.http.routers.appname.rule: "Host(`{{ mattermost_hostname }}.{{ ansible_nas_domain }}`)"
          traefik.http.routers.appname.tls.certresolver: "letsencrypt"
          traefik.http.routers.appname.tls.domains[0].main: "{{ ansible_nas_domain }}"
          traefik.http.routers.appname.tls.domains[0].sans: "*.{{ ansible_nas_domain }}"
          traefik.http.routers.appname.middlewares: "appname@docker{{ ',tinyauth-auth@docker' if mattermost_tinyauth_enabled | bool else '' }}"
          traefik.http.middlewares.appname.IPAllowList.sourcerange: "{{ mattermost_traefik_whitelist }}"
          traefik.http.services.appname.loadbalancer.server.port: "80"
### Docker hub example
          wud.tag.include: '^\d+\.\d+$'
          wud.tag.transform.type: "semver"
          wud.link.template: "https://hub.docker.com/r/devname/appname"
### Github example
#          wud.tag.include: '^\d+\.\d+\.\d+$'
#          wud.tag.transform.type: "semver"
#          wud.link.template: "https://github.com/dev/appname/pkgs/container/appname"
### Linuxserver.io example
#          wud.tag.include: '^\d+\.\d+\.\d+.*$'
#          wud.tag.transform.type: "semver"
#          wud.link.template: "https://lscr.io/linuxserver/appname"
        cpu_shares: "{{ mattermost_cpu_shares }}"
        memory: "{{ mattermost_memory }}"
        memory_swap: "{{ mattermost_memory_swap }}"
        restart_policy: unless-stopped

    - name: Access {{ ansible_nas_app_name }} at
      ansible.builtin.debug:
        msg: >-
              {{
                ['http://' + hostvars[inventory_hostname]['ansible_default_ipv4']['address'] + ':' + (mattermost_port_http | string)] +
                (['https://' + mattermost_hostname + '.' + ansible_nas_domain] if (mattermost_traefik_enabled is true or mattermost_dockflare_enabled is true) else [])
              }}
  when: mattermost_enabled is true

- name: Stop {{ ansible_nas_app_name }}
  block:
    - name: Stop {{ ansible_nas_app_name }} Container
      community.docker.docker_container:
        name: "{{ mattermost_container_name }}"
        state: absent
  when: mattermost_enabled is false

- name: Data Archive {{ ansible_nas_app_name }}
  include_role:
    name: ansible-nas-archive
  vars:
    target_data_dir: "{{ mattermost_data_directory }}"
  when:
    - archive_app_data_enabled is true
    - mattermost_enabled is false


## if setting up networks, add this to all containers:
        networks:                                  # optional
          - name: "{{ mattermost_network_name }}"     # optional
        network_mode: "{{ mattermost_network_name }}" # optional
        networks_cli_compatible: "yes"             # optional

##############################################################################
### EXAMPLES OF COMMON DATABASE SETUPS BELOW
### DELETE ALL OR PARTS BELOW AS NOT NEEDED
### PLEASE STICK TO THIS FORMAT
### NOTE: ../molecule files are not templated for DBs and
###       need to be updated as well when using DBs.
###       Look at the Nextcloud role for a good example.
##############################################################################


#################################################
###  OPTIONAL - remove if not needed          ###
###   __  __            _       ____  ____    ###
###  |  \/  | __ _ _ __(_) __ _|  _ \| __ )   ###
###  | |\/| |/ _` | '__| |/ _` | | | |  _ \   ###
###  | |  | | (_| | |  | | (_| | |_| | |_) |  ###
###  |_|  |_|\__,_|_|  |_|\__,_|____/|____/   ###
#################################################

    - name: Create {{ ansible_nas_app_name }} DB Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ mattermost_db_container_name }}"
        hostname: "{{ mattermost_db_container_name }}"
        image: "{{ mattermost_db_image_name }}:{{ mattermost_db_image_tag }}"
        pull: true
        volumes:
          - "{{ mattermost_data_directory }}/db:/var/lib/mysql"
        env:
          PUID: "1000"
          PGID: "1000"
          MYSQL_ROOT_PASSWORD: "topsecretroot"
          MYSQL_DATABASE: "appname"
          MYSQL_USER: "appname"
          MYSQL_PASSWORD: "{{ mattermost_db_password }}"
          TZ: "{{ ansible_nas_timezone }}"
        healthcheck:
              test:
                - CMD
                - /usr/local/bin/healthcheck.sh
                - --su-mysql
                - --connect
                - --innodb_initialized
              interval: "30s"
              timeout: "10s"
              retries: "3"
              start_period: "30s"
        labels:
          autoheal: "{{ mattermost_db_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ mattermost_db_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "false"
          homepage.name: "{{ ansible_nas_app_name }} MariaDB"
          homepage.group: "{{ mattermost_db_homepage_group }}"
          homepage.icon: "{{ mattermost_db_homepage_icon }}"
          homepage.href: "{{ mattermost_homepage_href }}"
          homepage.description: "{{ 'MariaDB Server is a high performing open source relational database, forked from MySQL.' if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ mattermost_db_container_name }}"
          homepage.showStats: "{{ mattermost_db_homepage_showstats | ternary('true', 'false') }}"
          repliqate.enabled: "{{ mattermost_db_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ mattermost_db_repliqate_engine }}"
          repliqate.schedule: "{{ mattermost_db_repliqate_schedule }}"
          repliqate.backup_id: "{{ mattermost_db_repliqate_backup_id }}"
          repliqate.retention: "{{ mattermost_db_repliqate_retention }}"
          traefik.enable: "false"
          wud.tag.include: '^\d+\.\d+\.\d+.*$'
          wud.tag.transform.type: "semver"
          wud.link.template: "https://hub.docker.com/_/mariadb"
        cpu_shares: "{{ mattermost_db_cpu_shares }}"
        memory: "{{ mattermost_db_memory }}"
        memory_swap: "{{ mattermost_db_memory_swap }}"
        restart_policy: unless-stopped


######################################################
###  OPTIONAL - remove if not needed               ###
###   __  __                         ____  ____    ###
###  |  \/  | ___  _ __   __ _  ___ |  _ \| __ )   ###
###  | |\/| |/ _ \| '_ \ / _` |/ _ \| | | |  _ \   ###
###  | |  | | (_) | | | | (_| | (_) | |_| | |_) |  ###
###  |_|  |_|\___/|_| |_|\__, |\___/|____/|____/   ###
###                      |___/                     ###
######################################################

    - name: Create {{ ansible_nas_app_name }} DB Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ mattermost_db_container_name }}"
        hostname: "{{ mattermost_db_container_name }}"
        image: "{{ mattermost_db_image_name }}:{{ mattermost_db_image_tag }}"
        pull: true
        volumes:
          - "{{ mattermost_data_directory }}/db:/data/db"
        env:
          TZ: "{{ ansible_nas_timezone }}"
        labels:
          autoheal: "{{ mattermost_db_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ mattermost_db_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "false"
          homepage.name: "{{ ansible_nas_app_name }} MongoDB"
          homepage.group: "{{ mattermost_db_homepage_group }}"
          homepage.icon: "{{ mattermost_db_homepage_icon }}"
          homepage.href: "{{ mattermost_homepage_href }}"
          homepage.description: "{{ 'The Database for Secure Apps' if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ mattermost_db_container_name }}"
          homepage.showStats: "{{ mattermost_db_homepage_showstats | ternary('true', 'false') }}"
          repliqate.enabled: "{{ mattermost_db_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ mattermost_db_repliqate_engine }}"
          repliqate.schedule: "{{ mattermost_db_repliqate_schedule }}"
          repliqate.backup_id: "{{ mattermost_db_repliqate_backup_id }}"
          repliqate.retention: "{{ mattermost_db_repliqate_retention }}"
          traefik.enable: "false"
          wud.tag.include: '^\d+\.\d+\.\d+.*$'
          wud.tag.transform.type: "semver"
          wud.link.template: "https://hub.docker.com/_/mongo"
        cpu_shares: "{{ mattermost_db_cpu_shares }}"
        memory: "{{ mattermost_db_memory }}"
        memory_swap: "{{ mattermost_db_memory_swap }}"
        restart_policy: unless-stopped


##########################################
###  OPTIONAL - remove if not needed   ###
###   __  __       ____   ___  _       ###
###  |  \/  |_   _/ ___| / _ \| |      ###
###  | |\/| | | | \___ \| | | | |      ###
###  | |  | | |_| |___) | |_| | |___   ###
###  |_|  |_|\__, |____/ \__\_\_____|  ###
###          |___/                     ###
##########################################

    - name: Create {{ ansible_nas_app_name }} DB Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ mattermost_db_container_name }}"
        hostname: "{{ mattermost_db_container_name }}"
        image: "{{ mattermost_db_image_name }}:{{ mattermost_db_image_tag }}"
        pull: true
        volumes:
          - "{{ mattermost_data_directory }}/mysql/mysql:/var/lib/mysql"
          - "{{ mattermost_data_directory }}/mysql/my.cnf:/etc/my.cnf"
        env:
          MYSQL_DATABASE: "appname"
          MYSQL_USER: "{{ mattermost_sql_user }}"
          MYSQL_PASSWORD: "{{ mattermost_sql_password }}"
          MYSQL_ROOT_PASSWORD: "{{ mattermost_sql_root_password }}"
          TZ: "{{ ansible_nas_timezone }}"
        labels:
          autoheal: "{{ mattermost_db_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ mattermost_db_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "false"
          homepage.name: "{{ ansible_nas_app_name }} MySQL"
          homepage.group: "{{ mattermost_db_homepage_group }}"
          homepage.icon: "{{ mattermost_db_homepage_icon }}"
          homepage.href: "{{ mattermost_homepage_href }}"
          homepage.description: "{{ 'MySQL Server, the world''s most popular open source database' if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ mattermost_db_container_name }}"
          homepage.showStats: "{{ mattermost_db_homepage_showstats | ternary('true', 'false') }}"
          repliqate.enabled: "{{ mattermost_db_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ mattermost_db_repliqate_engine }}"
          repliqate.schedule: "{{ mattermost_db_repliqate_schedule }}"
          repliqate.backup_id: "{{ mattermost_db_repliqate_backup_id }}"
          repliqate.retention: "{{ mattermost_db_repliqate_retention }}"
          traefik.enable: "false"
          wud.tag.include: '^\d+\.\d+\.\d+.*$'
          wud.tag.transform.type: "semver"
          wud.link.template: "https://hub.docker.com/_/mysql"
        cpu_shares: "{{ mattermost_db_cpu_shares }}"
        memory: "{{ mattermost_db_memory }}"
        memory_swap: "{{ mattermost_db_memory_swap }}"
        restart_policy: unless-stopped


##############################################################
###  OPTIONAL - remove if not needed                       ###
###   ____           _                 ____   ___  _       ###
###  |  _ \ ___  ___| |_ __ _ _ __ ___/ ___| / _ \| |      ###
###  | |_) / _ \/ __| __/ _` | '__/ _ \___ \| | | | |      ###
###  |  __/ (_) \__ \ || (_| | | |  __/___) | |_| | |___   ###
###  |_|   \___/|___/\__\__, |_|  \___|____/ \__\_\_____|  ###
###                     |___/                              ###
##############################################################

    - name: Create {{ ansible_nas_app_name }} DB Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ mattermost_db_container_name }}"
        hostname: "{{ mattermost_db_container_name }}"
        image: "{{ mattermost_db_image_name }}:{{ mattermost_db_image_tag }}"
        pull: true
        volumes:
          - "{{ mattermost_data_directory }}/db:/var/lib/postgresql/data"
        env:
          POSTGRES_USER: "appname"
          POSTGRES_PASSWORD: "appname"
          POSTGRES_DB: "appname"
          TZ: "{{ ansible_nas_timezone }}"
        labels:
          autoheal: "{{ mattermost_db_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ mattermost_db_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "false"
          homepage.name: "{{ ansible_nas_app_name }} PostgreSQL"
          homepage.group: "{{ mattermost_db_homepage_group }}"
          homepage.icon: "{{ mattermost_db_homepage_icon }}"
          homepage.href: "{{ mattermost_homepage_href }}"
          homepage.description: "{{ 'PostgreSQL Database Management System' if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ mattermost_db_container_name }}"
          homepage.showStats: "{{ mattermost_db_homepage_showstats | ternary('true', 'false') }}"
          repliqate.enabled: "{{ mattermost_db_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ mattermost_db_repliqate_engine }}"
          repliqate.schedule: "{{ mattermost_db_repliqate_schedule }}"
          repliqate.backup_id: "{{ mattermost_db_repliqate_backup_id }}"
          repliqate.retention: "{{ mattermost_db_repliqate_retention }}"
          traefik.enable: "false"
          wud.tag.include: '^\d+\.\d+.*$'
          wud.tag.transform.type: "semver"
          wud.link.template: "https://hub.docker.com/_/postgres"
        cpu_shares: "{{ mattermost_db_cpu_shares }}"
        memory: "{{ mattermost_db_memory }}"
        memory_swap: "{{ mattermost_db_memory_swap }}"
        restart_policy: unless-stopped


#########################################
###  OPTIONAL - remove if not needed  ###
###       ____          _ _           ###
###      |  _ \ ___  __| (_)___       ###
###      | |_) / _ \/ _` | / __|      ###
###      |  _ <  __/ (_| | \__ \      ###
###      |_| \_\___|\__,_|_|___/      ###
#########################################

    - name: Create {{ ansible_nas_app_name }} Redis Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ mattermost_redis_container_name }}"
        hostname: "{{ mattermost_redis_container_name }}"
        image: "{{ mattermost_redis_image_name }}:{{ mattermost_redis_image_tag }}"
        pull: true
        command: "redis-server --requirepass topsecret"
        env:
          TZ: "{{ ansible_nas_timezone }}"
        labels:
          autoheal: "{{ mattermost_redis_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ mattermost_redis_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "false"
          homepage.name: "{{ ansible_nas_app_name }} Redis"
          homepage.group: "{{ mattermost_redis_homepage_group }}"
          homepage.icon: "{{ mattermost_redis_homepage_icon }}"
          homepage.href: "{{ mattermost_homepage_href }}"
          homepage.description: "{{ 'The Real-time Data Platform' if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ mattermost_redis_container_name }}"
          homepage.showStats: "{{ mattermost_redis_homepage_showstats | ternary('true', 'false') }}"
          repliqate.enabled: "{{ mattermost_redis_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ mattermost_redis_repliqate_engine }}"
          repliqate.schedule: "{{ mattermost_redis_repliqate_schedule }}"
          repliqate.backup_id: "{{ mattermost_redis_repliqate_backup_id }}"
          repliqate.retention: "{{ mattermost_redis_repliqate_retention }}"
          traefik.enable: "false"
          wud.tag.include: '^\d+\.\d+\.\d+.*$'
#          wud.tag.include: "^v?\\d+-alpine$"
          wud.tag.transform.type: "semver"
          wud.link.template: "https://hub.docker.com/_/redis"
        cpu_shares: "{{ mattermost_redis_cpu_shares }}"
        memory: "{{ mattermost_redis_memory }}"
        memory_swap: "{{ mattermost_redis_memory_swap }}"
        restart_policy: unless-stopped
