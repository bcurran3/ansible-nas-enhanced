---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove {{ ansible_nas_app_name }} app
      community.docker.docker_container:
        name: "{{ invoiceninja_app_container_name }}"
        state: absent
      register: result_app

    - name: Try and stop and remove {{ ansible_nas_app_name }} DB
      community.docker.docker_container:
        name: "{{ invoiceninja_db_container_name }}"
        state: absent
      register: result_db

    - name: Try and stop and remove {{ ansible_nas_app_name }}
      community.docker.docker_container:
        name: "{{ invoiceninja_web_container_name }}"
        state: absent
      register: result_web

    - name: Check {{ ansible_nas_app_name }} is stopped
      ansible.builtin.assert:
        that:
          - not result_app.changed
          - not result_db.changed
          - not result_web.changed
