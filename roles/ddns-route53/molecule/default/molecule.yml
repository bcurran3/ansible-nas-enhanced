---
provisioner:
  inventory:
    group_vars:
      all:
        ddns_route53_enabled: true
        ddns_route53_key_id: "abcdef"
        ddns_route53_secret_key: "ghijkl"
        ddns_route53_hosted_zone_id: "1000"
        ddns_route53_data_directory: "/tmp/route53"
platforms:
  - name: instance
    image: geerlingguy/docker-ubuntu2204-ansible:latest
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
      - /var/run/docker.sock:/var/run/docker.sock
      - /tmp:/tmp:rw
    privileged: true
    pre_build_image: true
