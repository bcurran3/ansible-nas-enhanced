---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Stop and remove {{ couchdb_proper_name }}
      community.docker.docker_container:
        name: "{{ couchdb_container_name }}"
        state: absent
      register: result

    - name: Check {{ couchdb_proper_name }} is stopped
      ansible.builtin.assert:
        that:
          - not result.changed
