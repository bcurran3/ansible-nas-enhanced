---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Get {{ paperless_ngx_proper_name }} container state
      community.docker.docker_container:
        name: "{{ paperless_ng_container_name }}"
      register: result

    - name: Get {{ paperless_ngx_proper_name }} DB container state
      community.docker.docker_container:
        name: "{{ paperless_ng_db_container_name }}"
      register: result_db

    - name: Get {{ paperless_ngx_proper_name }} Redis container state
      community.docker.docker_container:
        name: "{{ paperless_ng_redis_container_name }}"
      register: result_redis

    - name: Check if {{ paperless_ngx_proper_name }} containers are running
      ansible.builtin.assert:
        that:
          - result.container['State']['Status'] == "running"
          - result.container['State']['Restarting'] == false
          - result_db.container['State']['Status'] == "running"
          - result_db.container['State']['Restarting'] == false
          - result_redis.container['State']['Status'] == "running"
          - result_redis.container['State']['Restarting'] == false
