---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove {{ homebox_proper_name }}
      community.docker.docker_container:
        name: "{{ homebox_container_name }}"
        state: absent
      register: result

    - name: Check {{ homebox_proper_name }} is stopped
      ansible.builtin.assert:
        that:
          - not result.changed
