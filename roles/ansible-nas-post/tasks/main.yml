---
- name: Construct the final string for FQDN accessable apps
  ansible.builtin.set_fact:
    ansible_nas_enabled_apps_string: "{{ ansible_nas_enabled_apps_list | join('\n') }}"

- name: Print the report of FQDN accessable apps
  debug:
    msg: "{{ ansible_nas_enabled_apps_list }}"