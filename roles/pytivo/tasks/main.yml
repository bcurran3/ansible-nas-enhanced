---
- name: Start {{ ansible_nas_app_name }}
  block:
    - name: Create {{ ansible_nas_app_name }} Directories
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: 0777
      with_items:
        - "{{ pytivo_data_directory }}"
        - "{{ pytivo_data_directory }}/config"

    - name: Create {{ ansible_nas_app_name }} Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ pytivo_container_name }}"
        hostname: "{{ pytivo_container_name }}"
        image: "{{ pytivo_image_name }}:{{ pytivo_image_version }}"
        pull: true
        volumes:
          - "/etc/localtime:/etc/localtime:ro"
          - "{{ pytivo_data_directory }}/config:/config:rw"
          - "{{ pytivo_movies_directory }}:/movies:ro"
          - "{{ pytivo_tv_directory }}:/tv:ro"
          - "{{ pytivo_photos_directory }}:/photos:ro"
          - "{{ pytivo_music_directory }}:/music:ro"
          - "{{ pytivo_podcasts_directory }}:/podcasts:ro"
        ports:
          - "{{ pytivo_port_http }}:9032"
          - "2190:2190/udp"
        env:
          TZ: "{{ ansible_nas_timezone }}"
          PUID: "{{ pytivo_user_id }}"
          PGID: "{{ pytivo_group_id }}"
        labels:
          autoheal: "{{ pytivo_autoheal }}"
          com.centurylinklabs.watchtower.enable: "{{ pytivo_watchtower_enable }}"
          dockflare.enable: "{{ pytivo_dockflare_tunnel }}"
          dockflare.access.group: "{{ pytivo_dockflare_access_group }}"
          dockflare.hostname: "{{ pytivo_hostname }}.{{ ansible_nas_domain }}"
          dockflare.service: "http://{{ hostvars[inventory_hostname]['ansible_default_ipv4']['address'] }}:9032"
          homepage.name: "{{ ansible_nas_app_name }}"
          homepage.group: "{{ pytivo_homepage_group }}"
          homepage.icon: "{{ pytivo_homepage_icon }}"
          homepage.href: "{{ pytivo_homepage_href }}"
          homepage.description: "{{ 'pytivo description' if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ pytivo_container_name }}"
          homepage.showStats: "{{ pytivo_homepage_showstats }}"
          homepage.siteMonitor: "http://{{ hostvars[inventory_hostname]['ansible_default_ipv4']['address'] }}:9032"
          repliqate.enabled: "{{ pytivo_repliqate_enabled }}"
          repliqate.engine: "{{ pytivo_repliqate_engine }}"
          repliqate.schedule: "{{ pytivo_repliqate_schedule }}"
          repliqate.backup_id: "{{ pytivo_repliqate_backup_id }}"
          repliqate.retention: "{{ pytivo_repliqate_retention }}"
          traefik.enable: "{{ pytivo_available_externally | string }}"
          traefik.http.routers.pytivo.rule: "Host(`{{ pytivo_hostname }}.{{ ansible_nas_domain }}`)"
          traefik.http.routers.pytivo.tls.certresolver: "letsencrypt"
          traefik.http.routers.pytivo.tls.domains[0].main: "{{ ansible_nas_domain }}"
          traefik.http.routers.pytivo.tls.domains[0].sans: "*.{{ ansible_nas_domain }}"
          traefik.http.routers.pytivo.middlewares: "pytivo@docker{{ ',tinyauth-auth@docker' if pytivo_tinyauth_enabled | bool else '' }}"
          traefik.http.middlewares.pytivo.IPAllowList.sourcerange: "{{ pytivo_traefik_whitelist }}"
          traefik.http.services.pytivo.loadbalancer.server.port: "9032"
          wud.watch.digest: "true"
          wud.link.template: "https://hub.docker.com/r/pinion/docker-pytivo"
        cpu_shares: "{{ pytivo_cpu_shares }}"
        memory: "{{ pytivo_memory }}"
        restart_policy: unless-stopped

    - name: Access {{ ansible_nas_app_name }} at
      ansible.builtin.debug:
        msg: >-
              {{
                ['http://' + hostvars[inventory_hostname]['ansible_default_ipv4']['address'] + ':9032'] +
                (['https://' + pytivo_hostname + '.' + ansible_nas_domain] if (pytivo_available_externally is true or pytivo_dockflare_tunnel is true) else [])
              }}
  when: pytivo_enabled is true

- name: Stop {{ ansible_nas_app_name }}
  block:
    - name: Stop {{ ansible_nas_app_name }} Container
      community.docker.docker_container:
        name: "{{ pytivo_container_name }}"
        state: absent
  when: pytivo_enabled is false

- name: Data Archive {{ ansible_nas_app_name }}
  include_role:
    name: ansible-nas-archive
  vars:
    target_data_dir: "{{ pytivo_data_directory }}"
  when:
    - archive_app_data_enabled is true
    - pytivo_enabled is false