---
- name: Start {{ ansible_nas_app_name }}
  block:
    - name: Create {{ ansible_nas_app_name }} Directories
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
      with_items:
        - "{{ mkvtoolnix_data_directory }}"
        - "{{ mkvtoolnix_data_directory }}/config"

    - name: Set {{ ansible_nas_app_name }} FQDN var overrides
      ansible.builtin.set_fact:
        mkvtoolnix_homepage_href: "https://{{ mkvtoolnix_hostname }}.{{ ansible_nas_domain }}"
      when: mkvtoolnix_traefik_enabled | bool or mkvtoolnix_dockflare_enabled | bool

    - name: Create {{ ansible_nas_app_name }} Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ mkvtoolnix_container_name }}"
        hostname: "{{ mkvtoolnix_container_name }}"
        image: "{{ mkvtoolnix_image_name }}:{{ mkvtoolnix_image_tag }}"
        pull: true
        volumes:
          - "{{ mkvtoolnix_data_directory }}/config:/config:rw"
          - "{{ mkvtoolnix_storage_directory }}:/storage:rw"
        ports:
          - "{{ mkvtoolnix_port_http }}:5800"
        env:
          APP_NICENESS: "10"
          CLEAN_TMP_DIR: "1"
          USER_ID: "{{ mkvtoolnix_user_id }}"
          GROUP_ID: "{{ mkvtoolnix_group_id }}"
          TZ: "{{ ansible_nas_timezone }}"
        labels:
          autoheal: "{{ mkvtoolnix_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ mkvtoolnix_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "{{ mkvtoolnix_dockflare_enabled | ternary('true', 'false') }}"
          dockflare.access.group: "{{ mkvtoolnix_dockflare_access_group }}"
          dockflare.hostname: "{{ mkvtoolnix_hostname }}.{{ ansible_nas_domain }}"
          dockflare.service: "http://{{ hostvars[inventory_hostname]['ansible_default_ipv4']['address'] }}:{{ mkvtoolnix_port_http }}"
          homepage.name: "{{ ansible_nas_app_name }}"
          homepage.group: "{{ mkvtoolnix_homepage_group }}"
          homepage.icon: "{{ mkvtoolnix_homepage_icon }}"
          homepage.href: "{{ mkvtoolnix_homepage_href }}"
          homepage.siteMonitor: "http://{{ hostvars[inventory_hostname]['ansible_default_ipv4']['address'] }}:{{ mkvtoolnix_port_http }}"
          homepage.description: "{{ 'Creating and working with Matroska files' if homepage_show_description else omit }}"
          homepage.server: "my-docker"
          homepage.container: "{{ mkvtoolnix_container_name }}"
          homepage.showStats: "{{ mkvtoolnix_homepage_showstats | ternary('true', 'false') }}"
          repliqate.enabled: "{{ mkvtoolnix_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ mkvtoolnix_repliqate_engine }}"
          repliqate.schedule: "{{ mkvtoolnix_repliqate_schedule }}"
          repliqate.backup_id: "{{ mkvtoolnix_repliqate_backup_id }}"
          repliqate.retention: "{{ mkvtoolnix_repliqate_retention }}"
          traefik.enable: "{{ mkvtoolnix_traefik_enabled | ternary('true', 'false') }}"
          traefik.http.routers.mkvtoolnix.rule: "Host(`{{ mkvtoolnix_hostname }}.{{ ansible_nas_domain }}`)"
          traefik.http.routers.mkvtoolnix.tls.certresolver: "letsencrypt"
          traefik.http.routers.mkvtoolnix.tls.domains[0].main: "{{ ansible_nas_domain }}"
          traefik.http.routers.mkvtoolnix.tls.domains[0].sans: "*.{{ ansible_nas_domain }}"
          traefik.http.routers.mkvtoolnix.middlewares: "mkvtoolnix@docker{{ ',tinyauth-auth@docker' if mkvtoolnix_tinyauth_enabled | bool else '' }}"
          traefik.http.middlewares.mkvtoolnix.IPAllowList.sourcerange: "{{ mkvtoolnix_traefik_whitelist }}"
          traefik.http.services.mkvtoolnix.loadbalancer.server.port: "5800"
          wud.tag.include: '^v\d+\.\d+\.\d+$'
          wud.tag.transform.type: "semver"
          wud.link.template: "https://github.com/jlesage/docker-mkvtoolnix/pkgs/container/mkvtoolnix"
        cpu_shares: "{{ mkvtoolnix_cpu_shares }}"
        memory: "{{ mkvtoolnix_memory }}"
        memory_swap: "{{ mkvtoolnix_memory_swap }}"
        restart_policy: unless-stopped

    - name: Access {{ ansible_nas_app_name }} at
      ansible.builtin.debug:
        msg: >-
              {{
                ['http://' + hostvars[inventory_hostname]['ansible_default_ipv4']['address'] + ':' + (mkvtoolnix_port_http | string)] +
                (['https://' + mkvtoolnix_hostname + '.' + ansible_nas_domain] if (mkvtoolnix_traefik_enabled is true or mkvtoolnix_dockflare_enabled is true) else [])
              }}
  when: mkvtoolnix_enabled is true

- name: Stop {{ ansible_nas_app_name }}
  block:
    - name: Stop {{ ansible_nas_app_name }} Container
      community.docker.docker_container:
        name: "{{ mkvtoolnix_container_name }}"
        state: absent
  when: mkvtoolnix_enabled is false

- name: Data Archive {{ ansible_nas_app_name }}
  include_role:
    name: ansible-nas-archive
  vars:
    target_data_dir: "{{ mkvtoolnix_data_directory }}"
  when:
    - archive_app_data_enabled is true
    - mkvtoolnix_enabled is false