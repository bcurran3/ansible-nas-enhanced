---
- name: Start {{ ansible_nas_app_name }}
  block:
    - name: Create {{ ansible_nas_app_name }} Directories
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
      with_items:
        - "{{ odoo_data_directory }}/data"
        - "{{ odoo_data_directory }}/db"
        - "{{ odoo_data_directory }}/config"
        - "{{ odoo_data_directory }}/addons"

    - name: Set {{ ansible_nas_app_name }} FQDN var overrides
      ansible.builtin.set_fact:
        odoo_homepage_href: "https://{{ odoo_hostname }}.{{ ansible_nas_domain }}"
      when: odoo_traefik_enabled | bool or odoo_dockflare_enabled | bool

    - name: Create {{ ansible_nas_app_name }} DB Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ odoo_db_container_name }}"
        hostname: "{{ odoo_db_container_name }}"
        image: "{{ odoo_db_image_name }}:{{ odoo_db_image_tag }}"
        pull: true
        volumes:
          - "{{ odoo_data_directory }}/db:/var/lib/postgresql/data/pgdata"
        network_mode: bridge
        env:
          POSTGRES_DB: "{{ odoo_db_env_postgres_db }}"
          POSTGRES_USER: "{{ odoo_db_env_postgres_user }}"
          POSTGRES_PASSWORD: "{{ odoo_db_env_postgres_password }}"
          PGDATA: "{{ odoo_db_env_pgdata }}"
        healthcheck:
          test: ["CMD-SHELL", "pg_isready -U {{ odoo_db_env_postgres_user }}"]
          interval: 10s
          timeout: 5s
          retries: 5
          start_period: 30s
        labels:
          autoheal: "{{ odoo_db_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ odoo_db_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "false"
          homepage.name: "{{ ansible_nas_app_name }} PostgreSQL"
          homepage.group: "{{ odoo_db_homepage_group }}"
          homepage.icon: "{{ odoo_db_homepage_icon }}"
          homepage.description: "{{ 'PostgreSQL Database Management System' if homepage_show_description else omit }}"
          homepage.showStats: "{{ odoo_db_homepage_showstats | ternary('true', 'false') }}"
          repliqate.enabled: "{{ odoo_db_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ odoo_db_repliqate_engine }}"
          repliqate.schedule: "{{ odoo_db_repliqate_schedule }}"
          repliqate.backup_id: "{{ odoo_db_repliqate_backup_id }}"
          repliqate.retention: "{{ odoo_db_repliqate_retention }}"
          traefik.enable: "false"
          wud.tag.include: '^\d+\.\d+.*$'
          wud.tag.transform.type: "semver"
          wud.link.template: "https://hub.docker.com/_/postgres"
        cpu_shares: "{{ odoo_db_cpu_shares }}"
        memory: "{{ odoo_db_memory }}"
        memory_swap: "{{ odoo_db_memory_swap }}"
        restart_policy: unless-stopped

    - name: Verify {{ ansible_nas_app_name }} DB is healthy
      community.docker.docker_container_info:
        name: "{{ odoo_db_container_name }}"
      register: db_info
      until: "db_info.container.State.Health.Status == 'healthy'"
      retries: 30
      delay: 5

    - name: Create {{ ansible_nas_app_name }} Container
      community.docker.docker_container:
        container_default_behavior: "compatibility"
        name: "{{ odoo_container_name }}"
        hostname: "{{ odoo_container_name }}"
        image: "{{ odoo_image_name }}:{{ odoo_image_tag }}"
        pull: true
        user: root
        links:
          - "{{ odoo_db_container_name }}"
        ports:
          - "{{ odoo_port_http }}:8069"
          - "{{ odoo_port_chat }}:8072"
        volumes:
          - "{{ odoo_data_directory }}/data:/var/lib/odoo"  # needed?
          - "{{ odoo_data_directory }}/config:/etc/odoo"
          - "{{ odoo_data_directory }}/addons:/mnt/extra-addons"
        network_mode: bridge
        env:
          HOST: "{{ odoo_db_container_name }}"
          USER: "{{ odoo_db_env_postgres_user }}"
          PASSWORD: "{{ odoo_db_env_postgres_password }}"
          TZ: "{{ ansible_nas_timezone }}"
        labels:
          autoheal: "{{ odoo_autoheal_enabled | ternary('true', 'false') }}"
          com.centurylinklabs.watchtower.enable: "{{ odoo_watchtower_enabled | ternary('true', 'false') }}"
          dockflare.enable: "{{ odoo_dockflare_enabled | ternary('true', 'false') }}"
          dockflare.access.group: "{{ odoo_dockflare_access_group }}"
          dockflare.hostname: "{{ odoo_hostname }}.{{ ansible_nas_domain }}"
          dockflare.service: "http://{{ hostvars[inventory_hostname]['ansible_default_ipv4']['address'] }}:{{ odoo_port_http }}"
          homepage.name: "{{ ansible_nas_app_name }}"
          homepage.group: "{{ odoo_homepage_group }}"
          homepage.icon: "{{ odoo_homepage_icon }}"
          homepage.href: "{{ odoo_homepage_href }}"
          homepage.description: "{{ 'Open Source Apps To Grow Your Business.' if homepage_show_description else omit }}"
          homepage.siteMonitor: "http://{{ hostvars[inventory_hostname]['ansible_default_ipv4']['address'] }}:{{ odoo_port_http }}"
          homepage.server: "my-docker"
          homepage.container: "odoo"
          homepage.showStats: "{{ odoo_homepage_showstats | ternary('true', 'false') }}"
          repliqate.enabled: "{{ odoo_repliqate_enabled | ternary('true', 'false') }}"
          repliqate.engine: "{{ odoo_repliqate_engine }}"
          repliqate.schedule: "{{ odoo_repliqate_schedule }}"
          repliqate.backup_id: "{{ odoo_repliqate_backup_id }}"
          repliqate.retention: "{{ odoo_repliqate_retention }}"
          traefik.enable: "{{ odoo_traefik_enabled | ternary('true', 'false') }}"
          traefik.http.routers.odoo.rule: "Host(`{{ odoo_hostname }}.{{ ansible_nas_domain }}`)"
          traefik.http.routers.odoo.tls.certresolver: "letsencrypt"
          traefik.http.routers.odoo.tls.domains[0].main: "{{ ansible_nas_domain }}"
          traefik.http.routers.odoo.tls.domains[0].sans: "*.{{ ansible_nas_domain }}"
          traefik.http.routers.odoo.middlewares: "odoo@docker{{ ',tinyauth-auth@docker' if odoo_tinyauth_enabled | bool else '' }}"
          traefik.http.middlewares.odoo.IPAllowList.sourcerange: "{{ odoo_traefik_whitelist }}"
          traefik.http.services.odoo.loadbalancer.server.port: "8069"
          wud.tag.include: '^\d+\.\d+.*$'
          wud.tag.transform.type: "semver"
          wud.link.template: "https://hub.docker.com/_/odoo"
        cpu_shares: "{{ odoo_cpu_shares }}"
        memory: "{{ odoo_memory }}"
        memory_swap: "{{ odoo_memory_swap }}"
        restart_policy: unless-stopped

    - name: Access {{ ansible_nas_app_name }} at
      ansible.builtin.debug:
        msg: >-
              {{
                ['http://' + hostvars[inventory_hostname]['ansible_default_ipv4']['address'] + ':' + (odoo_port_http | string)] +
                (['https://' + odoo_hostname + '.' + ansible_nas_domain] if (odoo_traefik_enabled is true or odoo_dockflare_enabled is true) else [])
              }}
  when: odoo_enabled is true

- name: Stop {{ ansible_nas_app_name }}
  block:
    - name: Stop {{ ansible_nas_app_name }} Container
      community.docker.docker_container:
        name: "{{ odoo_container_name }}"
        state: absent

    - name: Stop {{ ansible_nas_app_name }} DB Container
      community.docker.docker_container:
        name: "{{ odoo_db_container_name }}"
        state: absent
  when: odoo_enabled is false

- name: Data Archive {{ ansible_nas_app_name }}
  include_role:
    name: ansible-nas-archive
  vars:
    target_data_dir: "{{ odoo_data_directory }}"
  when:
    - archive_app_data_enabled is true
    - odoo_enabled is false